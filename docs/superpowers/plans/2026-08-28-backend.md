# Backend `photo_ui` — plan d'implémentation

> **Pour les agents exécutants :** SOUS-COMPÉTENCE REQUISE — utiliser
> `superpowers:subagent-driven-development` (recommandé) ou
> `superpowers:executing-plans` pour dérouler ce plan tâche par tâche. Les
> étapes sont en cases à cocher (`- [ ]`).

**But :** servir `docs/api-contract.md` depuis PostgreSQL, à partir des quatre
bases SQLite du pipeline lues en lecture seule, avec la cascade de datation
matérialisée à l'import et l'export d'un dossier de tâche sur disque.

**Architecture :** couches `controllers → metier → repository → db`, sans saut.
Composition root explicite dans `bootstrap.ts`, aucun conteneur de DI.
Mono-thread : le seul parallélisme est celui des processus enfants `sips` et de
l'entrée-sortie asynchrone. La règle capitale — une inférence ne doit jamais
ressembler à une lecture — est tenue par une **colonne générée** que PostgreSQL
refuse de laisser écrire, pas par une convention.

**Pile :** Node 24 · TypeScript strict · Fastify · `pg` + `pg-copy-streams` ·
`node:sqlite` (lecture seule) · `sips` · Vitest.

**Spécification :** `docs/backend-spec.md` (le comment) et
`docs/api-contract.md` (le quoi, **gelé**). Les deux voyagent avec ce plan ;
un exécutant lit les trois.

---

## Contraintes globales

Ces valeurs sont reprises **verbatim** des documents normatifs. Elles font
implicitement partie des exigences de chaque tâche.

| Contrainte | Valeur exacte |
|:---|:---|
| PostgreSQL | **17.6** — mesuré, conteneur Docker `timescaledb`, `localhost:5432` |
| Base | `photo_ui`, utilisateur `nico`, mot de passe `Funiculi`, collation ICU `fr-FR` |
| Extensions | `postgis 3.5.3`, `pg_trgm 1.6`, `unaccent 1.1` — **déjà installées** |
| Chargement en masse | `COPY … FROM STDIN` **uniquement**. `COPY … FROM '<chemin>'` est interdit : Postgres est dans un conteneur et ne voit pas `/Volumes/OWC Envoy Ultra` |
| Unicode | **NFC**, toute chaîne lue en amont, sans exception. `albumPath` arrive en NFD |
| Dates de prise de vue | `timestamp` sans fuseau + `int NULL` + `text`. **Jamais `timestamptz`.** `capture_offset_min` NULL ≠ 0 |
| Dates civiles résolues | `date`. Le recouvrement se calcule **au jour civil** |
| Horodatages d'audit | `timestamptz` — ce sont de vrais instants |
| Clés durables | `cloud_asset_id` (32 hex) et `sha256` (64 hex). **Jamais `photos.id`** |
| Clé d'un texte | le **couple** `(kind, id)`. 456 identifiants collisionnent — **vérifié** |
| Clés étrangères | **aucune** de `app` ou `ref` vers `pipeline` |
| Requêtes | paramétrées `$1, $2…`, y compris les listes (`= ANY($1::text[])`). Jamais d'interpolation |
| Écritures disque | toutes par `io/safe_fs.ts`. Racines inscriptibles : `RENDER_CACHE_ROOT`, `TASKS_ROOT` (+ `ANNOTATIONS_DIR` si `FEATURE_DATING_EXPORT`) |
| Jamais écrit | `/Volumes/OWC Envoy Ultra` en entier, caches compris · les 4 SQLite · `adobe_mcp/docs/` |
| Journalisation | service `Log` injecté. **Jamais `console.log`** hors amorçage (avec `eslint-disable` et son motif) |
| Jamais journalisé | le contenu des textes et des notes — ce sont des mémoires personnelles |
| Casse | API JSON `camelCase` · colonnes PostgreSQL `snake_case` · manifeste exporté `snake_case` |
| Rendu | `sips`, `execFile` avec tableau d'arguments, **jamais `exec` ni de shell** — les chemins contiennent des espaces |
| Parallélisme de rendu | **8** — mesuré : 59 ms en séquentiel, 19 ms à 8, facteur 3 |
| Périmètre | paramètre, pas constante : `PERIOD_FROM=1998-01-01`, `PERIOD_TO=2004-12-31`, 5 sets |
| Branche | `test_dev`. **Jamais `main`, jamais de `push`** |

---

## Décisions d'architecture, et leurs raisons

Écrites ici pour survivre au contexte qui les a prises. Un successeur reprend
sans re-décider.

### D1 — `server/` isolé, avec son propre `package.json`

**Convenu avec `impl-frontend` le 2026-08-28.** Le dépôt porte déjà un frontend
Vite dont le `package.json` est à la racine. Le backend vit **entièrement** sous
`server/`, avec son propre manifeste, son `tsconfig.json` et son
`vitest.config.ts`.

*Pourquoi :* deux jeux de dépendances dans un seul `package.json` produisent un
conflit sur `package-lock.json` à chaque `npm install` de l'un ou de l'autre —
le conflit de fusion le plus pénible qui soit, et le plus fréquent. Des
workspaces npm à la racine résoudraient le problème mais déplaceraient le
`package.json` du frontend, remaniement refusé à ce stade.

*Conséquence :* la disposition de `docs/backend-spec.md` §2.1 (`src/runtime/`,
`src/http/`…) est décalée sous `server/src/`. **C'est la seule divergence de
disposition, et elle est sans effet sur le découpage en couches.**

### D2 — Les valeurs codées ont une seule définition, les interfaces peuvent en avoir deux

`server/` importe `src/shared/enums.ts` et `src/shared/date_interface.ts` **en
lecture seule**, par l'alias `@shared/*`. Les interfaces que le frontend n'a pas
encore transcrites sont transcrites dans `server/src/contract/`.

*Pourquoi cette asymétrie :* une valeur codée transcrite deux fois peut diverger
**silencieusement** (`passage_date_from` contre `passage_date` : les deux
compilent, un seul est juste). Une interface transcrite deux fois ne peut pas :
le client valide en `zod.strictObject` et **lève** en nommant le champ. Le
contrat a prévu ce détecteur ; on s'appuie dessus pour les formes, on refuse d'y
recourir pour les valeurs.

### D3 — `node:sqlite`, pas `better-sqlite3`

**Vérifié** sur Node 24.18 : `new DatabaseSync(path, { readOnly: true })` ouvre
`documents.db` et lit ses 1 859 passages.

*Pourquoi :* supprime une dépendance native à compiler. Rejoint la réserve de la
question ouverte n° 3 de la spec sur `sharp` et Capacitor.

### D4 — PostgreSQL 17.6 (question ouverte n° 1 de la spec, tranchée)

*Pourquoi :* rien dans la spec n'exige la 18, et la base **existe déjà** en
17.6. La spec demandait de trancher avant la première migration parce que
revenir en arrière sur une base peuplée coûte plus cher que le contraire.

### D5 — L'import est **une** transaction (question ouverte n° 2, défaut (a))

*Pourquoi :* `TRUNCATE` est transactionnel en PostgreSQL ; un import qui échoue
à mi-parcours laisse la base exactement dans son état précédent, sans reprise ni
nettoyage. Le prix est un verrou de plusieurs minutes sur `pipeline`, accepté :
l'utilisateur déclenche lui-même l'import et sait qu'il tourne.

### D6 — `app.text_search` est une vue matérialisée (question ouverte n° 4)

*Pourquoi :* la frontière `pipeline` / `app` vaut plus que le coût d'un
`REFRESH` sur 2 871 lignes. **À vérifier à l'implémentation** (incertitude n° 6
de la spec) : si `REFRESH … CONCURRENTLY` est refusé dans la transaction
d'écriture d'une correction, replier sur un `REFRESH` non concurrent, qui pose
un verrou exclusif de quelques millisecondes.

### D7 — Le DDL de la spec est **vérifié**, plus supposé

Exécuté le 2026-08-28 sur la vraie base, dans une transaction, schéma jetable
nettoyé après. Lève l'**incertitude n° 1** de `docs/backend-spec.md` §17 :

| Vérifié | Résultat |
|:---|:---|
| `CREATE TEXT SEARCH CONFIGURATION fr_unaccent` + `ALTER MAPPING` | OK |
| Colonne générée `to_tsvector('fr_unaccent', …) STORED` | **acceptée** |
| `daterange` généré + index GiST | OK |
| `CHECK` mois-entier / année-entière avec `extract` | OK, et **rejettent** `[2004-09-14, 2004-09-14]` en `precision: 'month'` |
| `UPDATE … SET resolved_kind = 'reading'` | **`ERROR: column "resolved_kind" can only be updated to DEFAULT`** |
| `resolved_range && daterange('2000-12-01','2000-12-20','[]')` | ramène l'album au mois entier — le cas des 273 |
| `U&'Alge\0300s' = 'Algès'` | **faux** (6 caractères contre 5) ; `normalize(…, NFC)` répare |

Chiffres normatifs revérifiés sur les vraies bases : **456** collisions
`passages.id` ∩ `log_entries.id`, **82 albums / 3 930 photos** sur les cinq sets.

### D8 — Le manifeste suit les valeurs du contrat — **tranché par `contrat-api`**

L'annexe C de `docs/frontend-spec.md` écrivait `passage_date`,
`logbook-interpolated`, `date-range` et `rule: "B"` là où le contrat §2 dit
`passage_date_from`, `logbook_interpolated`, `date_range` et `passage`.

**Le contrat gagne, dans le manifeste aussi.** Deux raisons, la seconde de
`contrat-api` et plus forte que la mienne :

1. Une table de correspondance de **valeurs** est ce que la spec §4.3 refuse
   déjà pour `human`/`annotation` — « un endroit où se glisse une faute, pour un
   bénéfice nul ». On ne peut pas unifier là et se diviser ici.
2. **`"rule": "B"` trahit l'autosuffisance du manifeste.** « B » renvoie à une
   section de spec que le lecteur du dossier livré n'a pas, alors que la
   propriété n° 4 du contrat §7.4 exige un dossier déplaçable et envoyable tel
   quel. `"passage"` se comprend seul.

**Et une règle de forme, désormais explicite** — ce que j'avais pris pour une
incohérence n'en était pas une :

| Forme | Sens | Où |
|:---|:---|:---|
| `{start, end, precision, kind, source, bracket_hours}` | un `ResolvedDate` — le système **affirme** | `images[].date`, `texts[].date` |
| `{from, to}` | un intervalle simple, qui **n'affirme rien** | `task.period` (une question), `texts[].overlap` (une fenêtre calculée) |

Donc `texts[].date` passe en `start`/`end` **et porte `precision`** (`'day'`
dans tous les cas connus — raison de la rendre constante, pas de l'omettre) et
`bracket_hours: null`. `texts[].overlap` garde `from`/`to`.

*Conséquence :* `metier/export/` ne contient **qu'une conversion de casse et un
ordre de clés**. C'est exactement ce qu'un test d'idempotence peut couvrir.

### D10 — Le `CHECK` de précision teste l'alignement des bornes, pas la largeur

**Tranché : `contrat-api` a repris le correctif mot pour mot dans
`backend-spec.md` §4.3.** Le `CHECK` de la spec **rejetait l'exemple phare de la
spec elle-même** — mesuré :

`ref.album_span` sur `1998-02-Maison rose Algès` vaut `[1998-02-01, 1999-06-30]`
(dix-sept mois). Une photo de cet album sans EXIF est au rang 5,
`resolved_from = 'album_month'`, intervalle = le span saisi. Aucune valeur de
`resolved_precision` n'est jouable :

| Précision | Résultat |
|:---|:---|
| `'month'` | **rejetée** — `photo_month_is_whole_month` exige `end = start + 1 mois − 1 jour` |
| `'year'` | **rejetée** — le `CHECK` année exige une année civile pleine |
| `'day'` | acceptée par la base, mais `formatResolvedDate` rend `date.start` seul : la photo s'afficherait **`1998-02-01`**, un jour inventé au backend — ce que la règle 4 de « Besoins pour le backend » interdit |

*Le diagnostic :* le `CHECK` mesure **la largeur** alors que le contrat §2.2 dit
que `precision` qualifie **chaque borne**. Il confond les deux notions que le
contrat sépare. Pour un span saisi, les deux bornes sont connues au mois :
`precision: 'month'` est la description juste, la largeur est libre.

```sql
CONSTRAINT photo_month_is_whole_month CHECK (
  resolved_precision <> 'month' OR (
    extract(day from resolved_start) = 1
    AND resolved_end = (date_trunc('month', resolved_end::timestamp)
                        + interval '1 month' - interval '1 day')::date)),
CONSTRAINT photo_year_is_whole_year CHECK (
  resolved_precision <> 'year' OR (
    extract(doy from resolved_start) = 1
    AND resolved_end = (date_trunc('year', resolved_end::timestamp)
                        + interval '1 year' - interval '1 day')::date))
```

**Vérifié sur la base.** Le correctif **ne perd rien** de ce que le `CHECK`
protégeait :

| Cas | Strict (spec) | Aligné (retenu) |
|:---|:---|:---|
| `[1998-02-01, 1999-06-30]` en `month` — le span saisi | rejeté | **accepté** |
| `[2000-12-01, 2000-12-31]` en `month` — le mois simple | accepté | accepté |
| `[2004-09-14, 2004-09-14]` en `month` — le jour arbitraire | rejeté | **rejeté** |

*Question corollaire, tranchée aussi :* pas de sixième valeur `album_span` dans
`DateSource`. Ma symétrie s'appuyait sur `ref.web_span = decision`, or **Nicolas
a tranché depuis : `web_span` est une `inference`**. Le critère retenu :

> Ce qui distingue une décision d'une inférence n'est pas **qui** a agi mais
> **ce que le geste établit**. Une annotation de datation *arbitre* — quelqu'un
> a vu l'EXIF et a tapé autre chose. Une plage *comble un vide*.

Un `ref.album_span` comble un vide comme un `web_span` : la personne borne un
album dont on ne sait rien de précis, et le résultat est un intervalle au mois.
Donc `album_month` / `inference`. **`annotation` est la seule source de nature
`decision`** — le `CASE` SQL ne pose `decision` que là.

### D11 — Un texte affirme un jour, ou rien

**Trois contraintes ajoutées par `contrat-api`** sur `pipeline.text_unit`, prises
dans la migration initiale (base non peuplée, donc aucun `ALTER TABLE`) :

```sql
CONSTRAINT text_date_source_is_a_reading CHECK (
  date_source IS NULL OR date_source IN ('passage_date_from','log_entry_date')),
CONSTRAINT text_date_is_a_single_day CHECK (date_start = date_end),
CONSTRAINT text_date_complete CHECK ((date_source IS NULL) = (date_start IS NULL))
```

`date_kind` généré perd deux branches : il ne produit plus que `'reading'` ou
NULL.

*Pourquoi :* une fenêtre de page ou un `ref.web_span` est **ce qu'on a calculé
autour du texte**, pas ce qu'il dit. Ça vit dans `covers_*`, où
`page_span_source` le qualifie déjà. Avec ces contraintes, **toute date de texte
du système est une lecture, et la base le garantit** — la phrase « l'incertitude
est entièrement du côté des images » cesse d'être une approximation. Même
mécanisme que `resolved_kind` sur les photos.

*Conséquence sur l'import :* pour un passage sans `dateFrom`, laisser
`date_source`, `date_start`, `date_end` à **NULL** et ne remplir que
`covers_start` / `covers_end` / `covers_rule`.

*Conséquence sur le manifeste :* `texts[].date` peut être `null`, et c'est le
cas **normal** — plus d'un tiers des unités. Quand il ne l'est pas :
`start == end`, `precision: "day"`, `kind: "reading"`, `bracket_hours: null`.

**`page_window` et `web_span` restent des `DateSource` valides du contrat** — ils
qualifient `page.window_*` et le span d'un document web, pas `text_unit.date_*`.
Ne pas les retirer du vocabulaire partagé.

*Comptes vérifiés par moi sur `documents.db`, les huit tombent juste :*

| | Attendu | Mesuré |
|:---|---:|---:|
| total `text_unit` | 2 871 | **2 871** |
| passages · entrées | 1 859 · 1 012 | **1 859 · 1 012** |
| passages avec `dateFrom` | 828 | **828** |
| **avec une date** | 1 840 | **1 840** |
| **sans date** | 1 031 | **1 031** |
| dont web · dont datées par leur page | 569 · 462 | **569 · 462** |
| les 462, par `spanSource` | 341 `entries` + 121 `carried` | **341 + 121** |

### D9 — Ce qui n'est délibérément pas fait

ORM · framework de migration · `worker_threads` · cache applicatif · import
incrémental · matérialisation du recouvrement · `pgvector` · `ts_headline`
(le contrat exige des offsets, pas une chaîne pré-formatée) · authentification ·
lecture des catalogues Lightroom.

---

## Disposition des fichiers

```
server/
  package.json                 manifeste propre — aucune dépendance à la racine
  tsconfig.json                alias @shared/* -> ../src/shared/*
  vitest.config.ts             deux projets : unit (sans base) et integration (base réelle)
  .env.example
  src/
    runtime/
      config.ts                lecture et validation de l'environnement
      bootstrap.ts             LE composition root — le seul module qui construit
      server.ts                instanciation Fastify, arrêt propre
    log/
      log.ts                   le service de log — jamais console.log
    db/
      pool.ts
      transaction.ts           withTransaction — LA frontière transactionnelle
      migrate.ts               applique db/migrations/NNN_*.sql en ordre
    io/
      safe_fs.ts               LE point de contrôle des écritures disque
      sqlite_reader.ts         ouverture read-only des quatre bases
      sips.ts                  rendu d'image, execFile sans shell
    contract/                  les *_interface.ts que le frontend n'a pas transcrits
      error_interface.ts       AppError et le modèle d'erreur
      filter_interface.ts      ListEnvelope, FilterReport, TextRange, FieldMatch
      photo_interface.ts       PhotoListItem, PhotoDetail, Album, PhotoFacets
      text_interface.ts        TextRef, TextUnit, TextDocument, TextPage
      overlap_interface.ts     OverlapInfo, OverlapSummary
      task_interface.ts        TaskSummary, TaskDetail, mutations, export
      job_interface.ts         Job, ImportReport
      system_interface.ts      SystemStatus, RootStatus
    metier/
      dating/
        album_span.ts          rang 0 — préfixe d'album, suspected_range
        arbitration.ts         la fenêtre de 6 mois, en mois entiers
        cascade.ts             les six rangs — fonctions PURES, sans base
      overlap/
        overlap.ts             croisement d'intervalles, les deux largeurs
        covers.ts              les règles A, B, C
      search/
        clean_query.ts         nettoyage de q — NUL en premier
        highlight.ts           offsets UTF-16, calculés en Node
      tasks/
        slug.ts
        content_hash.ts        SHA-256 d'une sérialisation canonique
      export/
        canonical.ts           sérialisation déterministe
        manifest.ts            l'UNIQUE conversion camelCase -> snake_case
        export_service.ts
      images/
        image_service.ts       les trois échecs, le cache, le sémaphore de 8
      jobs/
        job_service.ts
    repository/
      photo_repository.ts      une méthode = une requête paramétrée
      text_repository.ts
      task_repository.ts
      ref_repository.ts
      import_repository.ts
    http/
      query_params.ts          l'allowlist stricte, UNE seule implémentation
      error_handler.ts         AppError -> ApiError du contrat
      photos_controller.ts
      texts_controller.ts
      tasks_controller.ts
      images_controller.ts
      jobs_controller.ts
      ref_controller.ts
      system_controller.ts
    import/
      import_service.ts        la passe, en une transaction
      read_index.ts            mcp-index.db
      read_content.ts          mcp-content.db
      read_documents.ts        documents.db
      read_dating.ts           dating.db
      read_annotations.ts      annotations.jsonl
      nfc.ts                   normalisation, appliquée sans exception
  db/
    migrations/
      001_extensions_and_schemas.sql
      002_pipeline.sql
      003_ref.sql
      004_app.sql
  test/
    fixtures/seed.sql          ~40 photos, une instance de CHAQUE piège
    helpers/db.ts              transaction annulée à la fin de chaque test
```

Tests **colocalisés** : `cascade.ts` → `cascade.test.ts` (unitaire),
`photo_repository.ts` → `photo_repository.itest.ts` (intégration).

---

## Le plan de tests

Livrable au même niveau que le plan d'implémentation.

### Deux projets Vitest, deux natures

| Projet | Fichiers | Base | Ce qu'il prouve |
|:---|:---|:---|:---|
| `unit` | `src/**/*.test.ts` | **aucune** | les règles pures : cascade, arbitrage, recouvrement, nettoyage de `q`, offsets, sérialisation canonique, `content_hash`, slug, allowlist |
| `integration` | `src/**/*.itest.ts` | **réelle** | le schéma, les requêtes, l'import, l'export, les invariants transverses |

La majeure partie de ce qui mérite un test est **pure** et ne touche à rien.
Aucune moquerie n'est nécessaire, et c'est le signe que le découpage est bon :
là où il faudrait moquer un `repository`, c'est qu'une règle a fui dans la
mauvaise couche.

### Le jeu d'essai d'intégration — une instance de chaque piège

`server/test/fixtures/seed.sql` construit une quarantaine de photos, trois
albums, deux documents et une trentaine de textes. La base de test n'est **pas**
une copie de la vraie : celle-ci fait 3 930 photos et vit sur un volume
démontable.

| Piège | Ce que le jeu d'essai contient |
|:---|:---|
| NFD / NFC | un `album_path` avec `Algès` décomposé |
| Collision d'identifiants | `logbook/p003/001` en `passage` **et** en `log_entry`, textes différents |
| Date de scan | un EXIF 2017 dans un album `2000-12` |
| Album à année seule | un album `2000` sans mois |
| Photo sans aucune date | une ligne `raw_date_source = 'none'` |
| **Le cas des 273** | un album au mois dont l'intervalle dépasse la borne haute d'un filtre |
| Fenêtre `carried` | une page dont `span_source = 'carried'` |
| `sha256` partagé | deux photos, un seul contenu, une seule vignette |
| Position absente mais nommée | une entrée de journal avec `place_name` et sans coordonnées |

**Cycle :** migrations appliquées une fois, puis **chaque test dans une
transaction annulée à la fin**. Isolation parfaite, pas de `TRUNCATE` entre les
tests, suite rapide.

### Les neuf invariants qui méritent leur propre fichier

Tests de **règle**, pas de fonction. Ils échouent quand quelqu'un a raison
localement et tort globalement.

1. **Une inférence ne peut jamais être servie comme une lecture** —
   `src/invariants/capital_rule.itest.ts`. Le test qui compte le plus du projet,
   à trois niveaux : *schéma* (l'`UPDATE` sur la colonne générée lève), *cascade*
   (les six rangs donnent exactement le `kind` annoncé, un `resolved_from`
   inconnu est rejeté par le `CHECK`), *contrat* (parcours récursif de chaque
   corps de réponse : **aucune clé nommée `date` ne porte une chaîne**, et tout
   objet portant `start` et `end` porte aussi `kind`, `source`, `precision`).
2. **Un filtre ne disparaît jamais** — paramètre mal orthographié → 400 en le
   nommant ; valeur inconnue d'un vocabulaire ouvert → 200, `total: 0`,
   `unmatchedValues` ; `q` réduit à rien → `total: 0` et **jamais** la
   population entière.
3. **Chevauchement, jamais inclusion** — le filtre `2000-12-01 → 2000-12-20`
   ramène l'album `[2000-12-01, 2000-12-31]`. Une lecture stricte en rend 0.
   C'est la régression la plus coûteuse possible, et elle est silencieuse.
4. **La clé d'un texte est le couple** — corriger `(passage, logbook/p003/001)`
   ne change rien à `(log_entry, logbook/p003/001)`, ni en base ni dans la
   réponse.
5. **L'import ne touche pas au travail humain** — importer, créer une tâche avec
   sélections, corrections et légendes, réimporter : tout est intact, les
   orphelins sont **marqués**, la table n'a pas perdu une ligne.
6. **Aucune FK de `app` vers `pipeline`** — un test interroge
   `information_schema` et échoue si une contrainte référentielle traverse les
   deux schémas. Bon marché, et empêche la régression qui détruirait le travail
   humain au prochain `TRUNCATE`.
7. **L'export est idempotent** — exporter deux fois, comparer les arbres octet
   par octet : seul `exported_at` diffère.
8. **Rien n'est écrit hors des racines inscriptibles** — empreinte de
   `ORIGINALS_ROOT` et `THUMBS_ROOT` relevée avant et après un rendu, une
   pré-construction et un export complets.
9. **NFC** — une recherche d'album tapée en NFC trouve l'album stocké depuis une
   source NFD.

### Ce qui n'est pas testé, et pourquoi

- **Le rendu `sips`** : c'est un binaire du système. On teste que le bon appel
  est construit et que les trois échecs sont distingués, pas que macOS sait
  redimensionner une image.
- **Les chiffres du corpus réel** (3 930, 2 424, 745…) : ce sont des mesures sur
  des données vivantes, pas des invariants du code. Les figer en assertions
  rendrait la suite rouge au premier import.
- **Le modèle de légendage** : testé à modèle moqué. La qualité d'une légende se
  juge à l'œil sur un échantillon.

### Couverture

Seuil **100 %** sur `src/metier/dating/**` et `src/metier/overlap/**` — ce sont
les règles dont une branche non exercée est une photo mal datée. **85 %** ailleurs,
hors `src/contract/**` (des types) et `runtime/bootstrap.ts` (du câblage).

---

## Tranche 0 — le socle

### Tâche 1 : le manifeste `server/`, le service de log, la configuration

**Fichiers :**
- Créer : `server/package.json`, `server/tsconfig.json`, `server/vitest.config.ts`, `server/.env.example`
- Créer : `server/src/log/log.ts`
- Créer : `server/src/runtime/config.ts`
- Test : `server/src/runtime/config.test.ts`

**Interfaces :**
- Produit : `createLog(level: LogLevel): Log` où
  `Log = { debug/info/warn/error(msg: string, fields?: Record<string, unknown>): void; child(fields): Log }`
- Produit : `loadConfig(env: NodeJS.ProcessEnv): Config` — **lève en nommant la
  variable qui manque**. `Config` porte `host`, `port`, `databaseUrl`,
  `originalsRoot`, `thumbsRoot`, `pipelineDbRoot`, `pagesRoot`, `annotationsDir`,
  `renderCacheRoot`, `tasksRoot`, `periodFrom`, `periodTo`, `perimeterSets: readonly string[]`,
  `renderEdge`, `renderConcurrency`, `featureDatingExport`, `logLevel`.

- [ ] **Étape 1 : écrire le test qui échoue**

```ts
// server/src/runtime/config.test.ts
import { describe, expect, test } from 'vitest';
import { loadConfig } from './config.ts';

const complete = {
  DATABASE_URL: 'postgres://nico@localhost:5432/photo_ui',
  ORIGINALS_ROOT: '/tmp/originals',
  THUMBS_ROOT: '/tmp/thumbs',
  PIPELINE_DB_ROOT: '/tmp/work',
  PAGES_ROOT: '/tmp/pages',
  ANNOTATIONS_DIR: '/tmp/annotations',
  RENDER_CACHE_ROOT: '/tmp/cache',
  TASKS_ROOT: '/tmp/tasks',
};

describe('loadConfig', () => {
  test('names the missing variable rather than failing vaguely', () => {
    const { RENDER_CACHE_ROOT: _omitted, ...incomplete } = complete;
    expect(() => loadConfig(incomplete)).toThrow(/RENDER_CACHE_ROOT/);
  });

  test('applies the documented defaults', () => {
    const config = loadConfig(complete);
    expect(config.host).toBe('127.0.0.1');
    expect(config.port).toBe(4310);
    expect(config.renderEdge).toBe(1400);
    expect(config.renderConcurrency).toBe(8);
    expect(config.featureDatingExport).toBe(false);
  });

  test('the perimeter is a parameter, not a constant', () => {
    const config = loadConfig({ ...complete, PERIOD_FROM: '1990-01-01', PERIOD_TO: '1995-12-31' });
    expect(config.periodFrom).toBe('1990-01-01');
    expect(config.periodTo).toBe('1995-12-31');
    expect(loadConfig(complete).perimeterSets)
      .toEqual(['1998-1999', '2000-2001', '2002', '2003', '2004']);
  });

  test('rejects a port that is not a number rather than coercing it to NaN', () => {
    expect(() => loadConfig({ ...complete, PHOTO_UI_PORT: 'quatre-mille' }))
      .toThrow(/PHOTO_UI_PORT/);
  });
});
```

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run src/runtime/config.test.ts`
Attendu : ÉCHEC — `Cannot find module './config.ts'`.

- [ ] **Étape 3 : écrire `server/package.json`**

```json
{
  "name": "photo-ui-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/runtime/main.ts",
    "start": "tsx src/runtime/main.ts",
    "test": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test:all": "vitest run",
    "test:coverage": "vitest run --project unit --coverage",
    "typecheck": "tsc --noEmit",
    "db:migrate": "tsx src/db/migrate_cli.ts",
    "db:import": "tsx src/import/import_cli.ts"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "pg": "^8.13.1",
    "pg-copy-streams": "^6.0.6",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "@types/pg-copy-streams": "^1.2.5",
    "@vitest/coverage-v8": "^4.1.11",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
```

- [ ] **Étape 4 : écrire `server/tsconfig.json`**

L'alias `@shared/*` est ce qui donne **une seule** définition aux valeurs codées (D2).

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["node", "vitest/globals"],
    "baseUrl": ".",
    "paths": { "@shared/*": ["../src/shared/*"] }
  },
  "include": ["src", "test", "vitest.config.ts"]
}
```

- [ ] **Étape 5 : écrire `server/vitest.config.ts`**

Deux projets : `unit` ne touche à aucune base, `integration` en a besoin.

```ts
import { defineConfig } from 'vitest/config';
import { fileURLToPath } from 'node:url';

const shared = fileURLToPath(new URL('../src/shared', import.meta.url));

export default defineConfig({
  resolve: { alias: { '@shared': shared } },
  test: {
    globals: true,
    projects: [
      {
        resolve: { alias: { '@shared': shared } },
        test: { name: 'unit', environment: 'node', include: ['src/**/*.test.ts'] },
      },
      {
        resolve: { alias: { '@shared': shared } },
        test: {
          name: 'integration',
          environment: 'node',
          include: ['src/**/*.itest.ts'],
          setupFiles: ['./test/helpers/setup_integration.ts'],
          fileParallelism: false,
          testTimeout: 30_000,
        },
      },
    ],
    coverage: {
      provider: 'v8',
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/**/*.itest.ts', 'src/contract/**',
                'src/runtime/bootstrap.ts', 'src/runtime/main.ts', 'src/**/*_cli.ts'],
      thresholds: {
        'src/metier/dating/**': { statements: 100, branches: 100, functions: 100, lines: 100 },
        'src/metier/overlap/**': { statements: 100, branches: 100, functions: 100, lines: 100 },
        statements: 85, branches: 85, functions: 85, lines: 85,
      },
    },
  },
});
```

- [ ] **Étape 6 : écrire `server/src/log/log.ts`**

Fastify embarque déjà pino ; le service l'enveloppe pour rester injectable et
pour que **rien d'autre du code ne connaisse le journaliseur**.

```ts
export const LogLevel = {
  DEBUG: 'debug', INFO: 'info', WARN: 'warn', ERROR: 'error',
} as const;
export type LogLevel = (typeof LogLevel)[keyof typeof LogLevel];

export interface Log {
  debug(message: string, fields?: Record<string, unknown>): void;
  info(message: string, fields?: Record<string, unknown>): void;
  warn(message: string, fields?: Record<string, unknown>): void;
  error(message: string, fields?: Record<string, unknown>): void;
  child(fields: Record<string, unknown>): Log;
}

const RANK: Record<LogLevel, number> = { debug: 10, info: 20, warn: 30, error: 40 };

export function createLog(level: LogLevel, base: Record<string, unknown> = {},
                          sink: (line: string) => void = writeLine): Log {
  const emit = (at: LogLevel, message: string, fields?: Record<string, unknown>): void => {
    if (RANK[at] < RANK[level]) return;
    sink(JSON.stringify({ at: new Date().toISOString(), level: at, message, ...base, ...fields }));
  };
  return {
    debug: (m, f) => emit(LogLevel.DEBUG, m, f),
    info: (m, f) => emit(LogLevel.INFO, m, f),
    warn: (m, f) => emit(LogLevel.WARN, m, f),
    error: (m, f) => emit(LogLevel.ERROR, m, f),
    child: (fields) => createLog(level, { ...base, ...fields }, sink),
  };
}

function writeLine(line: string): void {
  process.stdout.write(line + '\n');
}
```

- [ ] **Étape 7 : écrire `server/src/runtime/config.ts`**

```ts
export interface Config {
  readonly host: string;
  readonly port: number;
  readonly databaseUrl: string;
  readonly originalsRoot: string;
  readonly thumbsRoot: string;
  readonly pipelineDbRoot: string;
  readonly pagesRoot: string;
  readonly annotationsDir: string;
  readonly renderCacheRoot: string;
  readonly tasksRoot: string;
  readonly periodFrom: string;
  readonly periodTo: string;
  readonly perimeterSets: readonly string[];
  readonly renderEdge: number;
  readonly renderConcurrency: number;
  readonly featureDatingExport: boolean;
  readonly logLevel: LogLevel;
}

const DEFAULT_PERIMETER_SETS = ['1998-1999', '2000-2001', '2002', '2003', '2004'] as const;

export function loadConfig(env: NodeJS.ProcessEnv): Config {
  const missing: string[] = [];
  const required = (name: string): string => {
    const value = env[name];
    if (value === undefined || value.trim() === '') { missing.push(name); return ''; }
    return value;
  };
  const integer = (name: string, fallback: number): number => {
    const raw = env[name];
    if (raw === undefined || raw.trim() === '') return fallback;
    const parsed = Number(raw);
    if (!Number.isInteger(parsed)) {
      throw new Error(`${name} doit être un entier, reçu ${JSON.stringify(raw)}`);
    }
    return parsed;
  };

  const config: Config = {
    host: env.PHOTO_UI_HOST ?? '127.0.0.1',
    port: integer('PHOTO_UI_PORT', 4310),
    databaseUrl: required('DATABASE_URL'),
    originalsRoot: required('ORIGINALS_ROOT'),
    thumbsRoot: required('THUMBS_ROOT'),
    pipelineDbRoot: required('PIPELINE_DB_ROOT'),
    pagesRoot: required('PAGES_ROOT'),
    annotationsDir: required('ANNOTATIONS_DIR'),
    renderCacheRoot: required('RENDER_CACHE_ROOT'),
    tasksRoot: required('TASKS_ROOT'),
    periodFrom: env.PERIOD_FROM ?? '1998-01-01',
    periodTo: env.PERIOD_TO ?? '2004-12-31',
    perimeterSets: env.PERIMETER_SETS?.split(',').map((s) => s.trim())
      ?? [...DEFAULT_PERIMETER_SETS],
    renderEdge: integer('RENDER_EDGE', 1400),
    renderConcurrency: integer('RENDER_CONCURRENCY', 8),
    featureDatingExport: env.FEATURE_DATING_EXPORT === 'true',
    logLevel: (env.LOG_LEVEL ?? 'info') as LogLevel,
  };

  if (missing.length > 0) {
    throw new Error(`variables d'environnement manquantes : ${missing.join(', ')}`);
  }
  return config;
}
```

- [ ] **Étape 8 : lancer le test et le voir passer**

`cd server && npx vitest run src/runtime/config.test.ts` → PASS (4 tests).

- [ ] **Étape 9 : commiter**

```bash
git add server/package.json server/package-lock.json server/tsconfig.json \
        server/vitest.config.ts server/.env.example \
        server/src/log/log.ts server/src/runtime/config.ts server/src/runtime/config.test.ts
git commit -m "feat(server): manifest, log service and validated configuration"
```

---

### Tâche 2 : le pool, la frontière transactionnelle, le lanceur de migrations

**Fichiers :**
- Créer : `server/src/db/pool.ts`, `server/src/db/transaction.ts`, `server/src/db/migrate.ts`, `server/src/db/migrate_cli.ts`
- Créer : `server/test/helpers/db.ts`, `server/test/helpers/setup_integration.ts`
- Test : `server/src/db/transaction.itest.ts`

**Interfaces :**
- Consomme : `Config` et `Log` de la tâche 1.
- Produit : `createPool(databaseUrl: string): Pool`
- Produit : `withTransaction<T>(pool: Pool, fn: (client: PoolClient) => Promise<T>): Promise<T>`
  — `BEGIN`, appelle `fn`, `COMMIT` ; `ROLLBACK` sur toute exception **avant de la relancer**.
- Produit : `runMigrations(pool: Pool, log: Log, directory: string): Promise<string[]>`
  — rend les versions appliquées **pendant cet appel**.
- Produit (helper de test) : `withRollback(fn: (client: PoolClient) => Promise<void>): Promise<void>`

- [ ] **Étape 1 : écrire le test qui échoue**

```ts
// server/src/db/transaction.itest.ts
import { afterAll, expect, test } from 'vitest';
import { createPool } from './pool.ts';
import { withTransaction } from './transaction.ts';

const pool = createPool(process.env.DATABASE_URL_TEST!);
afterAll(async () => { await pool.end(); });

test('commits when the callback returns', async () => {
  await withTransaction(pool, async (client) => {
    await client.query('CREATE TEMP TABLE t_commit (v int)');
    await client.query('INSERT INTO t_commit VALUES ($1)', [1]);
  });
});

test('rolls back and rethrows when the callback throws', async () => {
  await withTransaction(pool, async (client) => {
    await client.query('CREATE TABLE public.t_rollback (v int)');
  }).catch(() => undefined);

  const boom = new Error('métier');
  await expect(withTransaction(pool, async (client) => {
    await client.query('INSERT INTO public.t_rollback VALUES (1)');
    throw boom;
  })).rejects.toBe(boom);

  const { rows } = await pool.query('SELECT count(*)::int AS n FROM public.t_rollback');
  expect(rows[0].n).toBe(0);
  await pool.query('DROP TABLE public.t_rollback');
});

test('releases the client back to the pool even after a failure', async () => {
  for (let i = 0; i < 30; i++) {
    await withTransaction(pool, async () => { throw new Error('boum'); }).catch(() => undefined);
  }
  const { rows } = await pool.query('SELECT 1 AS ok');
  expect(rows[0].ok).toBe(1);
});
```

*Le troisième test est celui qui compte : un client non relâché sur le chemin
d'erreur épuise le pool après N échecs, et le symptôme est un serveur qui se fige
sans message.*

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run --project integration src/db/transaction.itest.ts`
Attendu : ÉCHEC — `Cannot find module './pool.ts'`.

- [ ] **Étape 3 : écrire `pool.ts` et `transaction.ts`**

```ts
// server/src/db/pool.ts
import pg from 'pg';

// Une date PostgreSQL est un JOUR CIVIL, jamais un instant : sans ce parseur,
// node-postgres rend un objet Date construit dans le fuseau local, et une
// borne au 1er du mois recule d'un jour à l'ouest de Greenwich.
const DATE_OID = 1082;
pg.types.setTypeParser(DATE_OID, (value: string) => value);

export type Pool = pg.Pool;
export type PoolClient = pg.PoolClient;

export function createPool(databaseUrl: string): Pool {
  return new pg.Pool({ connectionString: databaseUrl, max: 10 });
}
```

```ts
// server/src/db/transaction.ts
import type { Pool, PoolClient } from './pool.ts';

/**
 * LA frontière transactionnelle. Un repository n'en ouvre jamais, un
 * contrôleur non plus : les erreurs remontent jusqu'ici, puis rollback.
 */
export async function withTransaction<T>(
  pool: Pool,
  fn: (client: PoolClient) => Promise<T>,
): Promise<T> {
  const client = await pool.connect();
  try {
    await client.query('BEGIN');
    const result = await fn(client);
    await client.query('COMMIT');
    return result;
  } catch (error) {
    await client.query('ROLLBACK').catch(() => undefined);
    throw error;
  } finally {
    client.release();
  }
}
```

- [ ] **Étape 4 : écrire `migrate.ts`**

Pas de framework : la seule fonction utile — ne pas rejouer ce qui est appliqué —
tient en vingt lignes.

```ts
// server/src/db/migrate.ts
import { readdir, readFile } from 'node:fs/promises';
import path from 'node:path';
import type { Log } from '../log/log.ts';
import type { Pool } from './pool.ts';
import { withTransaction } from './transaction.ts';

export async function runMigrations(pool: Pool, log: Log, directory: string): Promise<string[]> {
  await pool.query(`CREATE TABLE IF NOT EXISTS public.schema_migration (
    version text PRIMARY KEY, applied_at timestamptz NOT NULL DEFAULT now())`);

  const files = (await readdir(directory)).filter((f) => f.endsWith('.sql')).sort();
  const { rows } = await pool.query<{ version: string }>('SELECT version FROM public.schema_migration');
  const applied = new Set(rows.map((r) => r.version));
  const ran: string[] = [];

  for (const file of files) {
    const version = file.replace(/\.sql$/, '');
    if (applied.has(version)) continue;
    const sql = await readFile(path.join(directory, file), 'utf8');
    await withTransaction(pool, async (client) => {
      await client.query(sql);
      await client.query('INSERT INTO public.schema_migration (version) VALUES ($1)', [version]);
    });
    log.info('migration appliquée', { version });
    ran.push(version);
  }
  return ran;
}
```

- [ ] **Étape 5 : écrire les helpers de test**

```ts
// server/test/helpers/setup_integration.ts
// Refuse de lancer la suite d'intégration contre la base de travail : un
// TRUNCATE de test sur photo_ui détruirait un import de plusieurs minutes.
const url = process.env.DATABASE_URL_TEST;
if (!url) throw new Error('DATABASE_URL_TEST est requis pour la suite d\'intégration');
if (!/photo_ui_test/.test(url)) {
  throw new Error(`DATABASE_URL_TEST doit viser photo_ui_test, reçu ${url}`);
}
```

```ts
// server/test/helpers/db.ts
import { createPool, type Pool, type PoolClient } from '../../src/db/pool.ts';

let pool: Pool | undefined;
export function testPool(): Pool {
  pool ??= createPool(process.env.DATABASE_URL_TEST!);
  return pool;
}

/** Chaque test dans une transaction annulée : isolation parfaite, aucun TRUNCATE. */
export async function withRollback(fn: (client: PoolClient) => Promise<void>): Promise<void> {
  const client = await testPool().connect();
  try {
    await client.query('BEGIN');
    await fn(client);
  } finally {
    await client.query('ROLLBACK').catch(() => undefined);
    client.release();
  }
}
```

- [ ] **Étape 6 : créer la base de test et lancer le test**

```bash
PGPASSWORD=Funiculi createdb -h localhost -U nico photo_ui_test
cd server && DATABASE_URL_TEST='postgres://nico:Funiculi@localhost:5432/photo_ui_test' \
  npx vitest run --project integration src/db/transaction.itest.ts
```
Attendu : PASS (3 tests).

- [ ] **Étape 7 : commiter**

```bash
git add server/src/db server/test/helpers
git commit -m "feat(server): pool, transaction boundary and migration runner"
```

---

### Tâche 3 : les migrations, et les invariants que le schéma tient

C'est la tâche qui **transforme la règle capitale en propriété de la base**.
Le DDL est vérifié (D7) : le recopier fidèlement suffit.

**Fichiers :**
- Créer : `server/db/migrations/001_extensions_and_schemas.sql`
- Créer : `server/db/migrations/002_pipeline.sql`
- Créer : `server/db/migrations/003_ref.sql`
- Créer : `server/db/migrations/004_app.sql`
- Test : `server/src/invariants/schema.itest.ts`

**Interfaces :**
- Consomme : `runMigrations` de la tâche 2.
- Produit : les schémas `pipeline`, `app`, `ref` et la configuration de recherche
  `public.fr_unaccent`, tels que `docs/backend-spec.md` §4 les décrit.

- [ ] **Étape 1 : écrire les tests qui échouent**

```ts
// server/src/invariants/schema.itest.ts
import { beforeAll, expect, test } from 'vitest';
import { createLog, LogLevel } from '../log/log.ts';
import { runMigrations } from '../db/migrate.ts';
import { testPool, withRollback } from '../../test/helpers/db.ts';

beforeAll(async () => {
  await runMigrations(testPool(), createLog(LogLevel.ERROR), new URL('../../db/migrations', import.meta.url).pathname);
});

test('INVARIANT 1a — the capital rule is enforced by the engine, not by review', async () => {
  await withRollback(async (client) => {
    await client.query(`INSERT INTO pipeline.photo
      (cloud_asset_id, sha256, relative_path, file_name, format, raw_date_source,
       resolved_from, resolved_start, resolved_end, resolved_precision)
      VALUES ($1, $2, 'a/b.jpg', 'b.jpg', 'jpg', 'folder-month',
              'album_month', '2000-12-01', '2000-12-31', 'month')`,
      ['a'.repeat(32), 'b'.repeat(64)]);

    await expect(
      client.query(`UPDATE pipeline.photo SET resolved_kind = 'reading'`),
    ).rejects.toThrow(/can only be updated to DEFAULT/);
  });
});

test('INVARIANT 1b — an unknown resolved_from is refused, never a date without a nature', async () => {
  await withRollback(async (client) => {
    await expect(client.query(`INSERT INTO pipeline.photo
      (cloud_asset_id, sha256, relative_path, file_name, format, raw_date_source, resolved_from)
      VALUES ($1, $2, 'a/b.jpg', 'b.jpg', 'jpg', 'folder-month', 'album_week')`,
      ['c'.repeat(32), 'd'.repeat(64)]),
    ).rejects.toThrow(/photo_resolved_from_known/);
  });
});

test('a month-precision date IS the whole month, never an arbitrary day', async () => {
  await withRollback(async (client) => {
    await expect(client.query(`INSERT INTO pipeline.photo
      (cloud_asset_id, sha256, relative_path, file_name, format, raw_date_source,
       resolved_from, resolved_start, resolved_end, resolved_precision)
      VALUES ($1, $2, 'a/b.jpg', 'b.jpg', 'jpg', 'capture-date',
              'album_month', '2004-09-14', '2004-09-14', 'month')`,
      ['e'.repeat(32), 'f'.repeat(64)]),
    ).rejects.toThrow(/photo_month_is_whole_month/);
  });
});

test('INVARIANT 6 — no foreign key ever crosses from app or ref into pipeline', async () => {
  const { rows } = await testPool().query<{ constraint_name: string; from_schema: string; to_schema: string }>(`
    SELECT c.conname AS constraint_name,
           sf.nspname AS from_schema, st.nspname AS to_schema
      FROM pg_constraint c
      JOIN pg_class tf ON tf.oid = c.conrelid  JOIN pg_namespace sf ON sf.oid = tf.relnamespace
      JOIN pg_class tt ON tt.oid = c.confrelid JOIN pg_namespace st ON st.oid = tt.relnamespace
     WHERE c.contype = 'f' AND sf.nspname IN ('app','ref') AND st.nspname = 'pipeline'`);
  expect(rows).toEqual([]);
});
```

*L'invariant 6 est bon marché et empêche exactement la régression qui
détruirait le travail humain au prochain `TRUNCATE … CASCADE`.*

- [ ] **Étape 2 : lancer les tests et les voir échouer**

`cd server && npx vitest run --project integration src/invariants/schema.itest.ts`
Attendu : ÉCHEC — `relation "pipeline.photo" does not exist`.

- [ ] **Étape 3 : écrire `001_extensions_and_schemas.sql`**

```sql
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS unaccent;

-- La forme à DEUX arguments de to_tsvector avec un nom de configuration
-- littéral est obligatoire dans une colonne générée : to_tsvector(text) est
-- STABLE (elle dépend de default_text_search_config) et PostgreSQL la refuse.
CREATE TEXT SEARCH CONFIGURATION public.fr_unaccent (COPY = french);
ALTER TEXT SEARCH CONFIGURATION public.fr_unaccent
  ALTER MAPPING FOR hword, hword_part, word WITH unaccent, french_stem;

CREATE SCHEMA IF NOT EXISTS pipeline;
CREATE SCHEMA IF NOT EXISTS app;
CREATE SCHEMA IF NOT EXISTS ref;
```

- [ ] **Étape 4 : écrire `002_pipeline.sql`**

Transcrire **verbatim** `docs/backend-spec.md` §4.3 et §4.4 : `pipeline.photo`
avec ses colonnes générées `resolved_kind`, `resolved_range`, `search_meta`,
`search_ocr` et ses sept `CHECK` ; puis `album`, `photo_album`, `tag`,
`photo_tag`, `person`, `photo_person`, `dating_proposal`, `dating_doubt`,
`document`, `page`, `text_unit`, `import_run` ; puis les neuf index de §4.3 et
les trois de §4.4.

Deux points à ne pas rater :
- `dating_doubt.reason` n'a **aucun** `CHECK` — vocabulaire ouvert, il a déjà
  changé sous le projet.
- `text_unit` a `PRIMARY KEY (kind, id)` — **jamais `(id)`** : 456 identifiants
  collisionnent, et une clé sur l'id seul écraserait un texte par un autre.

- [ ] **Étape 5 : écrire `003_ref.sql` et `004_app.sql`**

Transcrire §4.5 et §4.6. **Aucune clé étrangère vers `pipeline`** dans l'un ni
dans l'autre : `app.task_image.cloud_asset_id` et `app.text_correction.(text_kind,
text_id)` sont des colonnes libres, et l'orphelinat se constate par jointure.

- [ ] **Étape 6 : lancer les tests et les voir passer**

`cd server && npx vitest run --project integration src/invariants/schema.itest.ts` → PASS (4 tests).

- [ ] **Étape 7 : commiter**

```bash
git add server/db/migrations server/src/invariants/schema.itest.ts
git commit -m "feat(server): schema migrations, with the capital rule held by a generated column"
```

---

### Tâche 4 : `safe_fs` — le point de contrôle unique des écritures

**Fichiers :**
- Créer : `server/src/io/safe_fs.ts`
- Test : `server/src/io/safe_fs.test.ts`

**Interfaces :**
- Produit : `createSafeFs(writableRoots: readonly string[], log: Log): SafeFs`, où
  `SafeFs = { assertWritable(target: string): void; writeFile(target, data): Promise<void>;
  mkdir(target): Promise<void>; rename(from, to): Promise<void>; rm(target): Promise<void>;
  createWriteStream(target): WriteStream }`
- Les racines sont **canonicalisées** au démarrage (`fs.realpath`), ce qui
  neutralise un lien symbolique pointant hors zone.

- [ ] **Étape 1 : écrire le test qui échoue**

```ts
// server/src/io/safe_fs.test.ts
import { mkdtemp, mkdir, symlink, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { beforeAll, expect, test } from 'vitest';
import { createLog, LogLevel } from '../log/log.ts';
import { createSafeFs } from './safe_fs.ts';

let cacheRoot: string; let originalsRoot: string; let safeFs: ReturnType<typeof createSafeFs>;

beforeAll(async () => {
  const base = await mkdtemp(path.join(tmpdir(), 'safefs-'));
  cacheRoot = path.join(base, 'cache');
  originalsRoot = path.join(base, 'originals');
  await mkdir(cacheRoot); await mkdir(originalsRoot);
  safeFs = await createSafeFs([cacheRoot], createLog(LogLevel.ERROR));
});

test('accepts a path inside a writable root', () => {
  expect(() => safeFs.assertWritable(path.join(cacheRoot, 'abc-1400.jpg'))).not.toThrow();
});

test('refuses a path outside every writable root', () => {
  expect(() => safeFs.assertWritable(path.join(originalsRoot, 'photo.jpg')))
    .toThrow(/écriture refusée hors racine/);
});

test('refuses a traversal that climbs out of the root', () => {
  expect(() => safeFs.assertWritable(path.join(cacheRoot, '..', 'originals', 'photo.jpg')))
    .toThrow(/écriture refusée hors racine/);
});

test('refuses a sibling whose name merely starts with the root', () => {
  expect(() => safeFs.assertWritable(cacheRoot + '-evil/x.jpg'))
    .toThrow(/écriture refusée hors racine/);
});

test('refuses a symlink that resolves outside the root', async () => {
  await writeFile(path.join(originalsRoot, 'real.jpg'), 'x');
  await symlink(originalsRoot, path.join(cacheRoot, 'escape'));
  await expect(safeFs.writeFile(path.join(cacheRoot, 'escape', 'real.jpg'), 'pwned'))
    .rejects.toThrow(/écriture refusée hors racine/);
});
```

*Le quatrième test est le piège classique de `startsWith` : `/tmp/cache-evil`
commence bien par `/tmp/cache`. Le cinquième est celui que `path.resolve` seul
ne rattrape pas — il faut résoudre le lien.*

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run src/io/safe_fs.test.ts`
Attendu : ÉCHEC — `Cannot find module './safe_fs.ts'`.

- [ ] **Étape 3 : écrire `safe_fs.ts`**

```ts
import { createWriteStream as nodeCreateWriteStream, type WriteStream } from 'node:fs';
import { mkdir, realpath, rename, rm, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { AppError } from '../contract/error_interface.ts';
import { ErrorCode } from '@shared/enums';
import type { Log } from '../log/log.ts';

export interface SafeFs {
  assertWritable(target: string): void;
  writeFile(target: string, data: string | Uint8Array): Promise<void>;
  mkdir(target: string): Promise<void>;
  rename(from: string, to: string): Promise<void>;
  rm(target: string): Promise<void>;
  createWriteStream(target: string): WriteStream;
}

export async function createSafeFs(writableRoots: readonly string[], log: Log): Promise<SafeFs> {
  // Canonicaliser AU DÉMARRAGE : un lien symbolique dans la racine elle-même
  // rendrait toute comparaison de préfixe fausse par la suite.
  const roots = await Promise.all(writableRoots.map((r) => realpath(r)));
  log.info('racines inscriptibles', { roots });

  const inside = (resolved: string): boolean =>
    roots.some((root) => resolved === root || resolved.startsWith(root + path.sep));

  const assertWritable = (target: string): void => {
    if (!inside(path.resolve(target))) {
      throw new AppError(ErrorCode.INTERNAL, `écriture refusée hors racine : ${target}`, 500,
                         { traceId: 'safe-fs' });
    }
  };

  // Un chemin peut être valide lexicalement et sortir par un lien symbolique.
  // On résout le plus long préfixe EXISTANT, puis on re-vérifie.
  const assertResolvedWritable = async (target: string): Promise<void> => {
    assertWritable(target);
    let probe = path.resolve(target);
    for (;;) {
      try { probe = await realpath(probe); break; } catch {
        const parent = path.dirname(probe);
        if (parent === probe) return;
        probe = parent;
      }
    }
    if (!inside(probe)) {
      throw new AppError(ErrorCode.INTERNAL, `écriture refusée hors racine : ${target}`, 500,
                         { traceId: 'safe-fs' });
    }
  };

  return {
    assertWritable,
    async writeFile(target, data) { await assertResolvedWritable(target); await writeFile(target, data); },
    async mkdir(target) { await assertResolvedWritable(target); await mkdir(target, { recursive: true }); },
    async rename(from, to) { await assertResolvedWritable(from); await assertResolvedWritable(to); await rename(from, to); },
    async rm(target) { await assertResolvedWritable(target); await rm(target, { recursive: true, force: true }); },
    createWriteStream(target) { assertWritable(target); return nodeCreateWriteStream(target); },
  };
}
```

- [ ] **Étape 4 : lancer le test et le voir passer**

`cd server && npx vitest run src/io/safe_fs.test.ts` → PASS (5 tests).

- [ ] **Étape 5 : commiter**

```bash
git add server/src/io/safe_fs.ts server/src/io/safe_fs.test.ts server/src/contract/error_interface.ts
git commit -m "feat(server): the single write checkpoint, with symlink escape refused"
```

---

### Tâche 5 : le modèle d'erreur et l'allowlist de paramètres

Ces deux-là vont ensemble : `query_params` est le principal producteur
d'`AppError`, et « un filtre ne disparaît jamais » (invariant 2) est ce qu'ils
prouvent à eux deux.

**Fichiers :**
- Créer : `server/src/contract/error_interface.ts` (complété depuis la tâche 4)
- Créer : `server/src/http/query_params.ts`
- Créer : `server/src/http/error_handler.ts`
- Test : `server/src/http/query_params.test.ts`

**Interfaces :**
- Produit : `class AppError extends Error { code: ErrorCode; httpStatus: number; details: unknown }`
- Produit : `parseQueryParams<S extends ParamSpec>(raw: Record<string, unknown>, spec: S): ParsedParams<S>`
  — lève `AppError(UNKNOWN_PARAMETER, 400)` en listant les noms fautifs **et**
  les noms acceptés ; lève `AppError(INVALID_PARAMETER, 400)` sur une valeur
  hors d'un vocabulaire fermé.
- Produit : `toApiError(error: unknown, traceId: string): { status: number; body: ApiError }`

- [ ] **Étape 1 : écrire le test qui échoue**

```ts
// server/src/http/query_params.test.ts
import { expect, test } from 'vitest';
import { PhotoScope, PhotoSort } from '@shared/enums';
import { parseQueryParams, type ParamSpec } from './query_params.ts';

const spec = {
  scope: { kind: 'closed', values: Object.values(PhotoScope), default: PhotoScope.HIERARCHY },
  sort: { kind: 'closed', values: Object.values(PhotoSort), default: PhotoSort.DATE_ASC },
  albumPath: { kind: 'openList' },
  dateFrom: { kind: 'isoDate' },
  reliableDatesOnly: { kind: 'boolean', default: false },
  limit: { kind: 'integer' },
} satisfies ParamSpec;

test('an unknown parameter name is a 400 that names it and lists what is accepted', () => {
  try {
    parseQueryParams({ albumPaht: 'x' }, spec);
    throw new Error('aurait dû lever');
  } catch (error) {
    const e = error as { code: string; httpStatus: number; details: { parameters: string[]; accepted: string[] } };
    expect(e.code).toBe('UNKNOWN_PARAMETER');
    expect(e.httpStatus).toBe(400);
    expect(e.details.parameters).toEqual(['albumPaht']);
    expect(e.details.accepted).toContain('albumPath');
  }
});

test('an invalid value in a CLOSED vocabulary is a 400 listing the accepted values', () => {
  try {
    parseQueryParams({ sort: 'weekly' }, spec);
    throw new Error('aurait dû lever');
  } catch (error) {
    const e = error as { code: string; details: { parameter: string; received: string; accepted: string[] } };
    expect(e.code).toBe('INVALID_PARAMETER');
    expect(e.details.parameter).toBe('sort');
    expect(e.details.received).toBe('weekly');
    expect(e.details.accepted).toContain('date_asc');
  }
});

test('an unknown value in an OPEN vocabulary is NOT an error — it restricts to zero', () => {
  const parsed = parseQueryParams({ albumPath: 'licorne' }, spec);
  expect(parsed.albumPath).toEqual(['licorne']);
});

test('repeated open values become a list', () => {
  expect(parseQueryParams({ albumPath: ['a', 'b'] }, spec).albumPath).toEqual(['a', 'b']);
});

test('reliableDatesOnly defaults to false — the doubt includes', () => {
  expect(parseQueryParams({}, spec).reliableDatesOnly).toBe(false);
});

test('a malformed date is a 400 naming the parameter, never a silently dropped filter', () => {
  expect(() => parseQueryParams({ dateFrom: '14/10/1999' }, spec)).toThrow(/dateFrom/);
});

test('applied filters are reported, so that no filter can vanish', () => {
  const parsed = parseQueryParams({ albumPath: 'x', dateFrom: '1999-01-01' }, spec);
  expect(parsed.applied.map((a) => a.parameter).sort()).toEqual(['albumPath', 'dateFrom']);
});
```

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run src/http/query_params.test.ts`
Attendu : ÉCHEC — `Cannot find module './query_params.ts'`.

- [ ] **Étape 3 : écrire `error_interface.ts`**

```ts
import type { ErrorCode } from '@shared/enums';

export class AppError extends Error {
  constructor(
    readonly code: ErrorCode,
    message: string,
    readonly httpStatus: number,
    readonly details: unknown,
  ) {
    super(message);
    this.name = 'AppError';
  }
}
```

- [ ] **Étape 4 : écrire `query_params.ts`**

**Une seule implémentation** pour tous les endpoints : c'est elle qui garantit
qu'un nom inconnu est un 400 nommé et non un filtre qui disparaît — la faute
qui, en amont, a deux fois renvoyé la bibliothèque entière.

```ts
import { ErrorCode } from '@shared/enums';
import { AppError } from '../contract/error_interface.ts';
import type { AppliedFilter } from '../contract/filter_interface.ts';

export type ParamRule =
  | { kind: 'closed'; values: readonly string[]; default?: string }
  | { kind: 'openList' }
  | { kind: 'open' }
  | { kind: 'isoDate' }
  | { kind: 'boolean'; default?: boolean }
  | { kind: 'integer' };
export type ParamSpec = Record<string, ParamRule>;

const ISO_DATE = /^\d{4}-\d{2}-\d{2}$/;

export function parseQueryParams(
  raw: Record<string, unknown>,
  spec: ParamSpec,
): Record<string, unknown> & { applied: AppliedFilter[] } {
  const accepted = Object.keys(spec).sort();
  const unknown = Object.keys(raw).filter((name) => !(name in spec));
  if (unknown.length > 0) {
    throw new AppError(ErrorCode.UNKNOWN_PARAMETER,
      `paramètre inconnu : ${unknown.join(', ')}`, 400,
      { parameters: unknown, accepted });
  }

  const invalid = (parameter: string, received: string, values: readonly string[] | null): never => {
    throw new AppError(ErrorCode.INVALID_PARAMETER,
      `valeur invalide pour ${parameter} : ${received}`, 400,
      { parameter, received, accepted: values });
  };

  const out: Record<string, unknown> = {};
  const applied: AppliedFilter[] = [];

  for (const [name, rule] of Object.entries(spec)) {
    const value = raw[name];
    if (value === undefined) {
      if (rule.kind === 'closed' && rule.default !== undefined) out[name] = rule.default;
      if (rule.kind === 'boolean' && rule.default !== undefined) out[name] = rule.default;
      continue;
    }
    switch (rule.kind) {
      case 'closed': {
        const s = String(value);
        if (!rule.values.includes(s)) invalid(name, s, rule.values);
        out[name] = s; applied.push({ parameter: name, values: [s], broadened: false });
        break;
      }
      case 'openList': {
        const list = (Array.isArray(value) ? value : [value]).map(String);
        out[name] = list; applied.push({ parameter: name, values: list, broadened: false });
        break;
      }
      case 'open': {
        const s = String(value);
        out[name] = s; applied.push({ parameter: name, values: [s], broadened: false });
        break;
      }
      case 'isoDate': {
        const s = String(value);
        if (!ISO_DATE.test(s) || Number.isNaN(Date.parse(s))) invalid(name, s, null);
        out[name] = s; applied.push({ parameter: name, values: [s], broadened: false });
        break;
      }
      case 'boolean': {
        const s = String(value);
        if (s !== 'true' && s !== 'false') invalid(name, s, ['true', 'false']);
        out[name] = s === 'true'; applied.push({ parameter: name, values: [s], broadened: false });
        break;
      }
      case 'integer': {
        const s = String(value);
        if (!/^\d+$/.test(s)) invalid(name, s, null);
        out[name] = Number(s); applied.push({ parameter: name, values: [s], broadened: false });
        break;
      }
    }
  }
  return { ...out, applied } as Record<string, unknown> & { applied: AppliedFilter[] };
}
```

- [ ] **Étape 5 : écrire `error_handler.ts`**

Toute exception non typée devient un `INTERNAL` avec un `traceId` : la trace
complète est journalisée, **rien du message d'origine ne part au client**.

```ts
import { randomUUID } from 'node:crypto';
import { ErrorCode } from '@shared/enums';
import { AppError } from '../contract/error_interface.ts';
import type { Log } from '../log/log.ts';

export function toApiError(error: unknown, log: Log): { status: number; body: unknown } {
  if (error instanceof AppError) {
    return { status: error.httpStatus,
             body: { error: { code: error.code, message: error.message, details: error.details } } };
  }
  const traceId = randomUUID();
  log.error('erreur non typée', { traceId, stack: error instanceof Error ? error.stack : String(error) });
  return { status: 500,
           body: { error: { code: ErrorCode.INTERNAL, message: 'erreur interne', details: { traceId } } } };
}
```

- [ ] **Étape 6 : lancer le test et le voir passer**

`cd server && npx vitest run src/http/query_params.test.ts` → PASS (7 tests).

- [ ] **Étape 7 : commiter**

```bash
git add server/src/http/query_params.ts server/src/http/query_params.test.ts \
        server/src/http/error_handler.ts server/src/contract
git commit -m "feat(server): error model and the strict parameter allowlist"
```

---

## Tranche 1 — la cascade et l'import

### Tâche 6 : rang 0 — l'intervalle d'un album

Calculé **d'abord, pour tous les albums**, parce que les rangs 2, 4, 5 et 6 s'en
servent. Fonctions **pures**, aucun accès base.

**Fichiers :**
- Créer : `server/src/metier/dating/album_span.ts`
- Test : `server/src/metier/dating/album_span.test.ts`

**Interfaces :**
- Produit : `parseAlbumPrefix(albumName: string): { year: number | null; month: number | null }`
- Produit : `albumInterval(albumName: string, refSpan: { from: string; to: string } | null): AlbumInterval | null`
  où `AlbumInterval = { from: string; to: string; presumed: boolean; precision: 'day' | 'month' | 'year' }`
- Produit : `isSuspectedRange(albumName: string): boolean`

**La règle de rang 0** (`docs/backend-spec.md` §7.3) :

```
si ref.album_span contient l'album  →  [date_from, date_to], presumed = false
sinon si le préfixe est `aaaa-NN` avec NN ∈ 01..12  →  le mois entier, presumed = true
sinon (NN > 12, ou année seule)     →  l'année entière, presumed = true
```

**Une précision que la spec ne donne pas et qu'il faut poser** (voir D10) : un
`ref.album_span` saisi porte des bornes **quelconques** (`1998-02-15`), alors que
`precision: 'month'` exige des bornes alignées sur le mois. Règle retenue :

- `from === to` → `precision: 'day'`, intervalle tel quel (l'album tient en un jour).
- sinon → `precision: 'month'`, bornes **élargies** au mois entier
  (`1998-02-15 → 1998-02-01`, `1999-06-20 → 1999-06-30`).

Élargir est sûr — « ne jamais écarter ce qui pourrait correspondre » ; rétrécir
perdrait des photos. L'intervalle **exact tel qu'il a été tapé** reste dans
`ref.album_span`, que l'écran de réglage affiche.

- [ ] **Étape 1 : écrire le test qui échoue**

Les noms d'albums sont **réels**, relevés sur les 82 du périmètre.

```ts
// server/src/metier/dating/album_span.test.ts
import { describe, expect, test } from 'vitest';
import { albumInterval, isSuspectedRange, parseAlbumPrefix } from './album_span.ts';

describe('parseAlbumPrefix', () => {
  test('reads YYYY-NN as a month when NN is a real month', () => {
    expect(parseAlbumPrefix('1998-02-Maison rose Algès')).toEqual({ year: 1998, month: 2 });
    expect(parseAlbumPrefix('1998-07 Famille Trotobas Lisbonne')).toEqual({ year: 1998, month: 7 });
    expect(parseAlbumPrefix('1999-12 Capvert Guadeloupe')).toEqual({ year: 1999, month: 12 });
  });

  test('NN > 12 is a trip number, not a month — year only', () => {
    expect(parseAlbumPrefix('2002-38Dec02')).toEqual({ year: 2002, month: null });
  });

  test('a bare year is a year', () => {
    expect(parseAlbumPrefix('2000')).toEqual({ year: 2000, month: null });
  });

  test('a second number in the name is not a month', () => {
    expect(parseAlbumPrefix('2000-06-2 Vierges Americaines')).toEqual({ year: 2000, month: 6 });
  });

  test('a name with no prefix yields nothing rather than a guess', () => {
    expect(parseAlbumPrefix('Chapon à trier')).toEqual({ year: null, month: null });
  });
});

describe('albumInterval', () => {
  test('a month prefix gives the WHOLE month, presumed', () => {
    expect(albumInterval('2000-12-viree au Venezuela-3mois', null)).toEqual({
      from: '2000-12-01', to: '2000-12-31', presumed: true, precision: 'month',
    });
  });

  test('a year-only prefix gives the whole year, presumed', () => {
    expect(albumInterval('2002-38Dec02', null)).toEqual({
      from: '2002-01-01', to: '2002-12-31', presumed: true, precision: 'year',
    });
  });

  test('February is 28 or 29 days, never 30 — the month is a real month', () => {
    expect(albumInterval('1998-02-Maison rose Algès', null).to).toBe('1998-02-28');
    expect(albumInterval('2000-02-Saint Bart', null).to).toBe('2000-02-29');
  });

  test('a saisi span wins over the prefix and is NOT presumed', () => {
    expect(albumInterval('1998-02-Maison rose Algès', { from: '1998-02-01', to: '1999-06-30' }))
      .toEqual({ from: '1998-02-01', to: '1999-06-30', presumed: false, precision: 'month' });
  });

  test('a saisi span with ragged bounds is WIDENED to whole months, never narrowed', () => {
    expect(albumInterval('1998-02-Maison rose Algès', { from: '1998-02-15', to: '1999-06-20' }))
      .toEqual({ from: '1998-02-01', to: '1999-06-30', presumed: false, precision: 'month' });
  });

  test('a saisi span of a single day stays a day', () => {
    expect(albumInterval('1999-03 Maldives', { from: '1999-03-02', to: '1999-03-02' }))
      .toEqual({ from: '1999-03-02', to: '1999-03-02', presumed: false, precision: 'day' });
  });

  test('an album whose name carries no date has no interval — nothing is invented', () => {
    expect(albumInterval('Chapon à trier', null)).toBeNull();
  });
});

describe('isSuspectedRange — an aid to SORTING the settings screen, never a date source', () => {
  test('an explicit duration counts', () => {
    expect(isSuspectedRange('2000-12-viree au Venezuela-3mois')).toBe(true);
  });

  test('two toponyms count', () => {
    expect(isSuspectedRange('2004-01- Fort Lauderdale - Belize')).toBe(true);
    expect(isSuspectedRange('2003-11-Sorel-Beaufort-Fort Lauderdale')).toBe(true);
  });

  test('a single place does not', () => {
    expect(isSuspectedRange('2000-01-guadeloupe')).toBe(false);
    expect(isSuspectedRange('2003-02-Cap Canaveral')).toBe(false);
  });
});
```

> **Ce que l'heuristique rate, et pourquoi ce n'est pas grave.**
> `1998-02-Maison rose Algès` couvre dix-sept mois et `isSuspectedRange` rend
> `false` : un seul toponyme, aucune durée. C'est **attendu**. Cet album est
> rattrapé par l'autre indice, `AlbumSpanHints.fileNamePatterns` — 19 de ses 22
> fichiers s'appellent `98-99 maison rose Lisbonne (N).jpg`. Les deux indices
> sont complémentaires et aucun n'est une source de date.

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run src/metier/dating/album_span.test.ts`
Attendu : ÉCHEC — `Cannot find module './album_span.ts'`.

- [ ] **Étape 3 : écrire `album_span.ts`**

```ts
export interface AlbumInterval {
  readonly from: string;
  readonly to: string;
  readonly presumed: boolean;
  readonly precision: 'day' | 'month' | 'year';
}

// `(?!\d)` empêche `\d{1,2}` de mordre dans un nombre plus long : sans lui,
// un hypothétique `2000-2001` produirait un mois 20.
const PREFIX = /^(\d{4})(?:-(\d{1,2})(?!\d))?/;
const DURATION = /\d+\s*(mois|semaines?|jours?|ans?)/i;

export function parseAlbumPrefix(albumName: string): { year: number | null; month: number | null } {
  const match = PREFIX.exec(albumName);
  if (match === null) return { year: null, month: null };
  const year = Number(match[1]);
  const raw = match[2];
  if (raw === undefined) return { year, month: null };
  const month = Number(raw);
  // NN > 12 est un numéro de voyage ou de semaine, pas un mois.
  return { year, month: month >= 1 && month <= 12 ? month : null };
}

const pad = (n: number): string => String(n).padStart(2, '0');
const lastDayOfMonth = (year: number, month: number): number =>
  new Date(Date.UTC(year, month, 0)).getUTCDate();

function startOfMonth(iso: string): string { return `${iso.slice(0, 7)}-01`; }
function endOfMonth(iso: string): string {
  const year = Number(iso.slice(0, 4));
  const month = Number(iso.slice(5, 7));
  return `${iso.slice(0, 7)}-${pad(lastDayOfMonth(year, month))}`;
}

export function albumInterval(
  albumName: string,
  refSpan: { from: string; to: string } | null,
): AlbumInterval | null {
  // Rang 0 : la saisie humaine gagne sur le préfixe, et n'est jamais presumed.
  if (refSpan !== null) {
    if (refSpan.from === refSpan.to) {
      return { from: refSpan.from, to: refSpan.to, presumed: false, precision: 'day' };
    }
    // Élargi, jamais rétréci : rétrécir perdrait des photos.
    return {
      from: startOfMonth(refSpan.from), to: endOfMonth(refSpan.to),
      presumed: false, precision: 'month',
    };
  }

  const { year, month } = parseAlbumPrefix(albumName);
  if (year === null) return null;
  if (month === null) {
    return { from: `${year}-01-01`, to: `${year}-12-31`, presumed: true, precision: 'year' };
  }
  return {
    from: `${year}-${pad(month)}-01`,
    to: `${year}-${pad(month)}-${pad(lastDayOfMonth(year, month))}`,
    presumed: true, precision: 'month',
  };
}

/** Heuristique d'aide à la saisie. Elle TRIE la liste de l'écran de réglage. */
export function isSuspectedRange(albumName: string): boolean {
  if (DURATION.test(albumName)) return true;
  const body = albumName.replace(PREFIX, '').replace(/^[\s-]+/, '');
  const segments = body.split(/\s*-\s*|\s+et\s+/).map((s) => s.trim()).filter((s) => /\p{L}/u.test(s));
  return segments.length >= 2;
}
```

- [ ] **Étape 4 : lancer le test et le voir passer**

`cd server && npx vitest run src/metier/dating/album_span.test.ts` → PASS (15 tests).

- [ ] **Étape 5 : commiter**

```bash
git add server/src/metier/dating/album_span.ts server/src/metier/dating/album_span.test.ts
git commit -m "feat(dating): rank 0 — the album interval, widened never narrowed"
```

---

### Tâche 7 : la fenêtre d'arbitrage

**Fichiers :**
- Créer : `server/src/metier/dating/arbitration.ts`
- Test : `server/src/metier/dating/arbitration.test.ts`

**Interfaces :**
- Consomme : `AlbumInterval` de la tâche 6.
- Produit : `arbitrate(captureDateLocal: string | null, album: AlbumInterval): Arbitration | null`
  où `Arbitration = { outcome: 'accepted' | 'rejected'; gapMonths: number; exifDay: string }`
  — rend `null` quand il n'y a **aucun** EXIF : c'est le rang 5, et il ne se
  confond pas avec le rang 4.

**La règle** (`docs/backend-spec.md` §7.4) :

```
L'EXIF est retenu ⟺ il tombe dans l'intervalle de l'album élargi de 6 mois
                     de chaque côté.
```

Pour un album au mois, la comparaison se fait **en mois entiers** — l'album ne
prétend pas au jour. Pour un album à année seule, la fenêtre porte sur l'année.

L'écart est stocké **des deux côtés**, retenu comme écarté : c'est lui qui
distingue le rang 4 (« l'EXIF existait, c'est une date de scan ») du rang 5
(« il n'y avait pas d'EXIF »), que `resolved_from` seul ne distingue pas.

- [ ] **Étape 1 : écrire le test qui échoue**

```ts
// server/src/metier/dating/arbitration.test.ts
import { describe, expect, test } from 'vitest';
import type { AlbumInterval } from './album_span.ts';
import { arbitrate, monthsBetween } from './arbitration.ts';

const december2000: AlbumInterval =
  { from: '2000-12-01', to: '2000-12-31', presumed: true, precision: 'month' };
const year2002: AlbumInterval =
  { from: '2002-01-01', to: '2002-12-31', presumed: true, precision: 'year' };

describe('monthsBetween — whole months, because an album does not claim a day', () => {
  test('same month is zero', () => expect(monthsBetween('2000-12-04', '2000-12-28')).toBe(0));
  test('counts calendar months, not 30-day blocks', () => {
    expect(monthsBetween('2000-12-31', '2001-01-01')).toBe(1);
    expect(monthsBetween('2000-12-01', '2001-06-30')).toBe(6);
  });
  test('is unsigned — a gap is a distance', () => {
    expect(monthsBetween('2001-06-01', '2000-12-01')).toBe(6);
  });
});

describe('arbitrate', () => {
  test('no EXIF at all is rank 5, and says so with null — never a gap of 0', () => {
    expect(arbitrate(null, december2000)).toBeNull();
  });

  test('an EXIF inside the album month is accepted with a gap of 0', () => {
    expect(arbitrate('2000-12-14T10:22:03', december2000))
      .toEqual({ outcome: 'accepted', gapMonths: 0, exifDay: '2000-12-14' });
  });

  test('an EXIF 3 months out is still accepted — the window is 6 months each side', () => {
    expect(arbitrate('2001-03-02T08:00:00', december2000))
      .toEqual({ outcome: 'accepted', gapMonths: 3, exifDay: '2001-03-02' });
  });

  test('exactly 6 months is inside the window', () => {
    expect(arbitrate('2001-06-15T08:00:00', december2000)?.outcome).toBe('accepted');
  });

  test('7 months is outside', () => {
    expect(arbitrate('2001-07-15T08:00:00', december2000)?.outcome).toBe('rejected');
  });

  test('a 2017 scan date in a 2000 album is REJECTED, and the gap is kept', () => {
    const result = arbitrate('2017-04-11T09:15:00', december2000);
    expect(result?.outcome).toBe('rejected');
    expect(result?.gapMonths).toBe(196);
  });

  test('for a year-only album the window is the YEAR, not six months', () => {
    expect(arbitrate('2002-11-30T12:00:00', year2002)?.outcome).toBe('accepted');
    expect(arbitrate('2003-01-02T12:00:00', year2002)?.outcome).toBe('rejected');
  });

  test('the six upstream captureDate formats all parse to the same civil day', () => {
    for (const raw of ['2000-12-14T11:42:23', '2000-12-14T11:42:23Z',
                       '2000-12-14T11:42:23.36', '2000-12-14T11:42:23.000Z',
                       '2000-12-14T11:42:23+02:00', '2000-12-14T11:42:23.780+04:00']) {
      expect(arbitrate(raw, december2000)?.exifDay).toBe('2000-12-14');
    }
  });
});
```

> **Le dernier test est celui qui protège des milliers de photos.** Les six
> formats de `captureDate` coexistent dans une seule colonne amont, et 76 % n'ont
> **aucun** fuseau. Le jour civil se lit sur les dix premiers caractères de la
> chaîne, **jamais** par un `new Date(raw)` suivi d'un `getDate()` — qui
> convertirait `…T23:07:11+04:00` dans le fuseau de la machine et changerait le
> jour. C'est le décalage silencieux que la spec interdit.

- [ ] **Étape 2 : lancer le test et le voir échouer**

`cd server && npx vitest run src/metier/dating/arbitration.test.ts`
Attendu : ÉCHEC — `Cannot find module './arbitration.ts'`.

- [ ] **Étape 3 : écrire `arbitration.ts`**

```ts
import type { AlbumInterval } from './album_span.ts';

export interface Arbitration {
  readonly outcome: 'accepted' | 'rejected';
  readonly gapMonths: number;
  readonly exifDay: string;
}

const WINDOW_MONTHS = 6;

/** Nombre de mois CIVILS entre deux jours, non signé. */
export function monthsBetween(a: string, b: string): number {
  const months = (iso: string): number => Number(iso.slice(0, 4)) * 12 + Number(iso.slice(5, 7)) - 1;
  return Math.abs(months(a) - months(b));
}

/**
 * Le jour civil d'un captureDate amont, LU et jamais converti.
 * Six formats coexistent et 76 % n'ont aucun fuseau ; passer par `new Date`
 * décalerait le jour de ceux qui en ont un.
 */
function civilDay(captureDateLocal: string): string | null {
  const day = captureDateLocal.slice(0, 10);
  return /^\d{4}-\d{2}-\d{2}$/.test(day) ? day : null;
}

export function arbitrate(captureDateLocal: string | null, album: AlbumInterval): Arbitration | null {
  if (captureDateLocal === null) return null;      // rang 5 — pas d'EXIF, et ça se dit
  const exifDay = civilDay(captureDateLocal);
  if (exifDay === null) return null;

  if (album.precision === 'year') {
    const sameYear = exifDay.slice(0, 4) === album.from.slice(0, 4);
    return { outcome: sameYear ? 'accepted' : 'rejected',
             gapMonths: monthsBetween(exifDay, album.from), exifDay };
  }

  // Dans l'intervalle : écart nul. Sinon, la distance à la borne la plus proche.
  const gapMonths = exifDay < album.from ? monthsBetween(exifDay, album.from)
                  : exifDay > album.to   ? monthsBetween(exifDay, album.to)
                  : 0;
  return { outcome: gapMonths <= WINDOW_MONTHS ? 'accepted' : 'rejected', gapMonths, exifDay };
}
```

- [ ] **Étape 4 : lancer le test et le voir passer**

`cd server && npx vitest run src/metier/dating/arbitration.test.ts` → PASS (11 tests).

- [ ] **Étape 5 : commiter**

```bash
git add server/src/metier/dating/arbitration.ts server/src/metier/dating/arbitration.test.ts
git commit -m "feat(dating): the six-month arbitration window, in whole months"
```
