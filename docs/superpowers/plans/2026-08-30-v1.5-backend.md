# V1.5 — plan d'implémentation backend

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Servir ce dont la 1.5 a besoin — les quatre amendements au contrat, la date d'une page selon la cascade registre → notes → héritage, les vignettes de page, les dates proposées du site, et les facettes des filtres de textes.

**Architecture:** Rien de nouveau dans les couches. Les tables amont `pipeline.*` restent une copie fidèle de la pipeline et ne sont jamais recalculées ; tout ce que la 1.5 dérive vit dans le schéma `app`, recalculé après import et après chaque écriture qui l'invalide. Les contrôleurs gardent leur allowlist stricte de paramètres et leur traducteur d'erreurs.

**Tech Stack:** Node + TypeScript strict, Fastify 5, PostgreSQL (schémas `pipeline`, `ref`, `app`), Vitest (`*.test.ts` unitaires, `*.itest.ts` intégration sur base réelle), migrations SQL numérotées dans `server/db/migrations/`.

**Spec:** `docs/spec-v1.5.md`

## Global Constraints

- Aucun chemin en dur : tout passe par `server/src/runtime/config.ts`.
- `pipeline.*` et `ref.*` amont sont **en lecture** pour la 1.5 ; les dérivations nouvelles vont dans `app.*`.
- Requêtes paramétrées (`$1, $2…`), jamais d'interpolation de chaîne.
- Écritures liées dans une seule transaction (`withTransaction`).
- `Log` seulement, jamais `console.log`.
- Types stricts, pas de `any` hors donnée externe non typée.
- Tout paramètre de requête passe par `parseQueryParams` et son `ParamSpec` : un paramètre inconnu est un 400 nommé, jamais un filtre ignoré en silence.
- Chaque amendement au contrat est écrit dans `docs/api-contract.md` sous « Amendements depuis le gel » **dans la même tâche** que le code.
- Commandes : `npm test` (unitaires), `npm run test:integration`, `npm run typecheck`, depuis `server/`.

## Ce que ce plan ne fait pas

À dire une fois pour que personne ne l'invente :

- **Aucune optimisation anticipée.** 155 pages, 60 documents web, 82 albums : ce sont des tailles triviales. Pas de pagination, pas de cache mémoire, pas d'index inventé « au cas où » sur ces tables. La seule mise en cache du plan est celle des vignettes, et elle existe parce qu'on a mesuré 49 Mo d'images.
- **Aucune écriture vers la pipeline amont**, ni dans `adobe_mcp`, ni sur le volume externe.
- **Aucun découpage d'image.** La vignette est le scan entier réduit.
- **Aucune relecture d'appariement de galerie** : les légendes restent indicatives.
- **Aucune correction des dates de transcription suspectes** : elles sont signalées, pas réparées.

---

## Structure des fichiers

**Créés :**

| Fichier | Responsabilité |
|:--|:--|
| `server/db/migrations/006_v15.sql` | Les quatre amendements + `app.page_date` |
| `server/src/metier/pages/page_date.ts` | La cascade registre → notes → héritage, pure, testable sans base |
| `server/src/metier/pages/page_date.test.ts` | Ses tests unitaires |
| `server/src/repository/page_date_repository.ts` | Écrit `app.page_date`, appelé après import et après écriture de `web_span` |
| `server/src/repository/page_date_repository.itest.ts` | Ses tests d'intégration |
| `server/src/metier/pages/thumb_service.ts` | Réduction d'un scan, avec cache disque |
| `server/src/metier/pages/thumb_service.test.ts` | Ses tests unitaires |
| `server/src/repository/web_proposal_repository.ts` | La date proposée d'une page du site et ce qui la soutient |
| `server/src/repository/web_proposal_repository.itest.ts` | Ses tests d'intégration |
| `server/src/repository/text_facets.ts` | Années/mois/jours réellement présents par source |
| `server/src/repository/text_facets.itest.ts` | Ses tests d'intégration |
| `server/src/metier/tasks/note_title.ts` | Le préfixe d'attribution : fabrication et verrou |
| `server/src/metier/tasks/note_title.test.ts` | Ses tests unitaires |

**Modifiés :**

| Fichier | Changement |
|:--|:--|
| `server/src/contract/task_interface.ts` | `TaskNote.derivedFrom`/`editedSince`, `TaskPatchInput.exportDirectory` |
| `server/src/contract/text_interface.ts` | `TextPage.date`, `WebDocumentRow.proposal`, `TextFacets` |
| `server/src/http/tasks_controller.ts` | Verrou de titre, `exportDirectory` dans le patch |
| `server/src/http/texts_controller.ts` | `/pages/thumb`, `excludedCount` réel, `/texts/facets` |
| `server/src/http/ref_controller.ts` | `web-span` à borne unique, proposition servie |
| `server/src/repository/task_repository.ts` | Persistance des deux champs de note et du répertoire |
| `server/src/repository/text_repository.ts` | `excludedCount`, date de page servie |
| `server/src/import/import_service.ts` | Appelle le recalcul de `app.page_date` en fin d'import |

---

## Tranche A — Les quatre amendements au contrat

**Pourquoi en premier :** les deux agents en dépendent. Un contrat qui bouge sous le frontend a déjà coûté cher deux fois. Cette tranche se termine par une annonce aux deux agents ; le frontend ne code contre ces formes qu'après.

**Livrable :** le contrat écrit, les migrations passées, les quatre formes servies par le vrai serveur.

### Task 1 : La migration

**Files:**
- Create: `server/db/migrations/006_v15.sql`
- Test: `server/src/invariants/schema.itest.ts` (modifier)

**Interfaces:**
- Produces: les colonnes `app.task_note.derived_from_kind`, `derived_from_id`, `derived_text_original`; `app.task.export_directory` (existe déjà, aucune migration) ; `ref.web_span.date_to` devient nullable ; la table `app.page_date`.

- [ ] **Step 1: Écrire le test qui échoue**

Dans `server/src/invariants/schema.itest.ts`, ajouter :

```typescript
test('006 : app.page_date porte la date résolue de chaque page', async () => {
  const { rows } = await client.query(`
    SELECT column_name, data_type FROM information_schema.columns
     WHERE table_schema = 'app' AND table_name = 'page_date' ORDER BY column_name`);
  expect(rows.map((r) => r.column_name)).toEqual(
    ['date_end', 'date_start', 'page_id', 'source']);
});

test('006 : une note peut nommer le texte dont elle dérive', async () => {
  const { rows } = await client.query(`
    SELECT column_name, is_nullable FROM information_schema.columns
     WHERE table_schema = 'app' AND table_name = 'task_note'
       AND column_name LIKE 'derived%' ORDER BY column_name`);
  expect(rows).toEqual([
    { column_name: 'derived_from_id', is_nullable: 'YES' },
    { column_name: 'derived_from_kind', is_nullable: 'YES' },
    { column_name: 'derived_text_original', is_nullable: 'YES' },
  ]);
});

test('006 : ref.web_span accepte une borne de fin absente', async () => {
  const { rows } = await client.query(`
    SELECT is_nullable FROM information_schema.columns
     WHERE table_schema = 'ref' AND table_name = 'web_span' AND column_name = 'date_to'`);
  expect(rows[0].is_nullable).toBe('YES');
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- schema`
Expected: FAIL — `expected [] to equal [ 'date_end', ... ]`

- [ ] **Step 3: Écrire la migration**

`server/db/migrations/006_v15.sql` :

```sql
-- Amendement : une note peut dériver d'un texte d'époque recopié.
-- `derived_text_original` garde le texte AU MOMENT DE LA RECOPIE : le drapeau
-- « édité depuis » se calcule en le comparant au corps courant, jamais stocké
-- comme un booléen qui pourrait mentir après une écriture directe en base.
ALTER TABLE app.task_note ADD COLUMN derived_from_kind text;
ALTER TABLE app.task_note ADD COLUMN derived_from_id   text;
ALTER TABLE app.task_note ADD COLUMN derived_text_original text;
ALTER TABLE app.task_note ADD CONSTRAINT task_note_derived_complete
  CHECK (num_nonnulls(derived_from_kind, derived_from_id, derived_text_original) IN (0, 3));

-- Amendement : la période d'un document du site est une BORNE DE DÉBUT.
-- La fin est celle du document daté suivant, calculée à la lecture.
ALTER TABLE ref.web_span ALTER COLUMN date_to DROP NOT NULL;
ALTER TABLE ref.web_span DROP CONSTRAINT web_span_ordered;
ALTER TABLE ref.web_span ADD CONSTRAINT web_span_ordered
  CHECK (date_to IS NULL OR date_from <= date_to);

-- La date résolue d'une page, dérivée par photo_ui et jamais par la pipeline
-- amont : `pipeline.page.window_*` reste la copie fidèle de ce que la pipeline
-- a calculé, cette table porte la cascade de la 1.5.
CREATE TABLE app.page_date (
  page_id    text PRIMARY KEY REFERENCES pipeline.page(id) ON DELETE CASCADE,
  date_start date NOT NULL,
  date_end   date NOT NULL,
  source     text NOT NULL CHECK (source IN ('register', 'notes', 'carried')),
  CONSTRAINT page_date_ordered CHECK (date_start <= date_end)
);
```

- [ ] **Step 4: Appliquer et vérifier que le test passe**

Run: `cd server && npm run db:migrate && npm run test:integration -- schema`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/db/migrations/006_v15.sql server/src/invariants/schema.itest.ts
git commit -m "feat(db): les colonnes de la 1.5 — note dérivée, borne unique, date de page"
```

### Task 2 : La note dérivée et son drapeau « édité depuis »

**Files:**
- Modify: `server/src/contract/task_interface.ts`
- Modify: `server/src/repository/task_repository.ts`
- Modify: `server/src/http/tasks_controller.ts`
- Modify: `docs/api-contract.md`
- Test: `server/src/repository/task_texts_notes.itest.ts`

**Interfaces:**
- Consumes: les colonnes de la Task 1.
- Produces: `TaskNote.derivedFrom: TaskTextRef | null` et `TaskNote.editedSince: boolean` ; `TaskNoteCreateInput.derivedFrom?: TaskTextRef`.

- [ ] **Step 1: Écrire le test qui échoue**

Dans `server/src/repository/task_texts_notes.itest.ts` :

```typescript
test('une note dérivée nomme sa source et se dit non éditée tant que le texte ne bouge pas',
  async () => {
    const note = await createTaskNote(client, 'tache-a', {
      title: 'journal de bord, page 3 du 09/07/1998',
      text: 'Départ de Figueira.',
      attachedTo: { images: [], texts: [{ kind: 'passage', id: 'logbook/p003/001' }] },
      derivedFrom: { kind: 'passage', id: 'logbook/p003/001' },
    });
    expect(note.derivedFrom).toEqual({ kind: 'passage', id: 'logbook/p003/001' });
    expect(note.editedSince).toBe(false);
  });

test('retoucher le texte d’une note dérivée lève le drapeau — et ne le baisse jamais à tort',
  async () => {
    const note = await createTaskNote(client, 'tache-a', {
      title: 'journal de bord, page 3 du 09/07/1998',
      text: 'Départ de Figueira.',
      attachedTo: { images: [], texts: [] },
      derivedFrom: { kind: 'passage', id: 'logbook/p003/001' },
    });
    const edited = await patchTaskNote(client, 'tache-a', note.id, { text: 'Départ de Figueira, au moteur.' });
    expect(edited?.editedSince).toBe(true);

    const restored = await patchTaskNote(client, 'tache-a', note.id, { text: 'Départ de Figueira.' });
    expect(restored?.editedSince).toBe(false);
  });

test('une note écrite de zéro ne dérive de rien', async () => {
  const note = await createTaskNote(client, 'tache-a', {
    title: 'Ce que le journal ne dit pas',
    text: 'Gaëtan n’était plus à bord après 2002.',
    attachedTo: { images: [], texts: [] },
  });
  expect(note.derivedFrom).toBeNull();
  expect(note.editedSince).toBe(false);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- task_texts_notes`
Expected: FAIL — `derivedFrom` n'existe pas sur `TaskNote`

- [ ] **Step 3: Implémenter**

Dans `server/src/contract/task_interface.ts` :

```typescript
export interface TaskNote {
  readonly id: string;
  readonly title: string;
  readonly text: string;
  readonly createdAt: string;
  readonly attachedTo: { readonly images: readonly string[]; readonly texts: readonly TaskTextRef[] };
  /** Le texte d'époque recopié pour fabriquer cette note. `null` = écrite de zéro. */
  readonly derivedFrom: TaskTextRef | null;
  /**
   * Vrai quand le corps ne correspond plus au texte recopié. CALCULÉ à la
   * lecture par comparaison, jamais stocké : un booléen stocké mentirait après
   * une écriture directe en base. Toujours `false` si `derivedFrom` est `null`.
   */
  readonly editedSince: boolean;
}
```

Dans `TaskNoteCreateInput`, ajouter `readonly derivedFrom?: TaskTextRef;`.

Dans `server/src/repository/task_repository.ts`, la projection des notes devient :

```sql
SELECT n.id, n.title, n.body, n.created_at,
       n.derived_from_kind, n.derived_from_id,
       (n.derived_text_original IS NOT NULL AND n.body <> n.derived_text_original) AS edited_since
  FROM app.task_note n
 WHERE n.task_slug = $1
 ORDER BY n.created_at
```

et le mapping :

```typescript
derivedFrom: row.derived_from_kind === null || row.derived_from_id === null
  ? null
  : { kind: row.derived_from_kind, id: row.derived_from_id },
editedSince: row.edited_since,
```

À l'insertion, quand `derivedFrom` est fourni, lire le texte effectif du texte source et l'écrire dans `derived_text_original` **dans la même transaction** :

```sql
INSERT INTO app.task_note (id, task_slug, title, body, derived_from_kind, derived_from_id,
                           derived_text_original)
SELECT $1, $2, $3, $4, $5, $6,
       (SELECT coalesce(c.text, t.body) FROM pipeline.text_unit t
          LEFT JOIN app.text_correction c ON c.text_kind = t.kind AND c.text_id = t.id
         WHERE t.kind = $5 AND t.id = $6)
```

- [ ] **Step 4: Lancer le test, vérifier qu'il passe**

Run: `cd server && npm run test:integration -- task_texts_notes && npm run typecheck`
Expected: PASS, typecheck propre

- [ ] **Step 5: Écrire l'amendement au contrat**

Dans `docs/api-contract.md`, sous « Amendements depuis le gel », ajouter la ligne A4 : `TaskNote.derivedFrom` + `editedSince`, type modifié : oui, deux champs ajoutés. Une phrase de motif : *l'attribution que porte le titre n'est vérifiable par rien sans eux, et une note recopiée puis retravaillée cesse d'être une citation sans cesser d'y ressembler.*

- [ ] **Step 6: Commit**

```bash
git add server/src/contract/task_interface.ts server/src/repository/task_repository.ts \
        server/src/repository/task_texts_notes.itest.ts docs/api-contract.md
git commit -m "feat(server): une note dit de quel texte elle dérive et si elle a bougé"
```

### Task 3 : Le verrou de préfixe de titre

**Files:**
- Create: `server/src/metier/tasks/note_title.ts`
- Create: `server/src/metier/tasks/note_title.test.ts`
- Modify: `server/src/http/tasks_controller.ts`
- Test: `server/src/http/tasks_controller.itest.ts`

**Interfaces:**
- Consumes: `TaskNote.derivedFrom` (Task 2).
- Produces: `attributionPrefix(title: string): string | null` et `titleKeepsPrefix(current: string, next: string): boolean`.

- [ ] **Step 1: Écrire le test qui échoue**

`server/src/metier/tasks/note_title.test.ts` :

```typescript
import { attributionPrefix, titleKeepsPrefix } from './note_title.ts';

test('le préfixe d’attribution s’arrête au premier tiret cadratin', () => {
  expect(attributionPrefix('journal de bord, page 12 du 04/11/2003'))
    .toBe('journal de bord, page 12 du 04/11/2003');
  expect(attributionPrefix('journal de bord, page 12 du 04/11/2003 — la nuit du grain'))
    .toBe('journal de bord, page 12 du 04/11/2003');
  expect(attributionPrefix('site web, Vers Trinidad (1999-2002) — à relire'))
    .toBe('site web, Vers Trinidad (1999-2002)');
});

test('un titre sans préfixe d’attribution n’en a pas', () => {
  expect(attributionPrefix('Ce que le journal ne dit pas')).toBeNull();
});

test('on peut ajouter à la suite du préfixe, jamais l’altérer', () => {
  const current = 'journal de bord, page 12 du 04/11/2003';
  expect(titleKeepsPrefix(current, `${current} — la nuit du grain`)).toBe(true);
  expect(titleKeepsPrefix(current, current)).toBe(true);
  expect(titleKeepsPrefix(current, 'journal de bord, page 13 du 04/11/2003')).toBe(false);
  expect(titleKeepsPrefix(current, 'la nuit du grain')).toBe(false);
  expect(titleKeepsPrefix(current, '')).toBe(false);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm test -- note_title`
Expected: FAIL — module introuvable

- [ ] **Step 3: Implémenter**

`server/src/metier/tasks/note_title.ts` :

```typescript
/**
 * Le titre d'une note tirée d'un texte d'époque porte son attribution —
 * « journal de bord, page 12 du 04/11/2003 ». C'est le SEUL porteur de
 * provenance pour qui lit `textes/notes.md`, où le rattachement n'apparaît
 * pas. Le verrou est donc au serveur : `PATCH` accepte n'importe quel titre,
 * et une protection d'interface ne protégerait rien.
 *
 * Convention : le préfixe est ce qui précède le premier tiret cadratin.
 * L'utilisateur écrit ce qu'il veut après, jamais avant.
 */
const SOURCE_LABELS = ['journal de bord, ', 'ma vie, ', 'site web, '] as const;

export function attributionPrefix(title: string): string | null {
  const known = SOURCE_LABELS.some((label) => title.startsWith(label));
  if (!known) return null;
  const cut = title.indexOf(' — ');
  return (cut === -1 ? title : title.slice(0, cut)).trimEnd();
}

export function titleKeepsPrefix(current: string, next: string): boolean {
  const prefix = attributionPrefix(current);
  if (prefix === null) return true;
  return next === prefix || next.startsWith(`${prefix} — `);
}
```

- [ ] **Step 4: Lancer le test, vérifier qu'il passe**

Run: `cd server && npm test -- note_title`
Expected: PASS

- [ ] **Step 5: Brancher au contrôleur, avec son test d'intégration**

Dans `server/src/http/tasks_controller.itest.ts` :

```typescript
test('PATCH refuse un titre qui efface le préfixe d’attribution', async () => {
  const created = await app.server.inject({
    method: 'POST', url: '/tasks/tache-a/notes',
    payload: { title: 'journal de bord, page 3 du 09/07/1998', text: 'Départ.',
               attachedTo: { images: [], texts: [] },
               derivedFrom: { kind: 'passage', id: 'logbook/p003/001' } },
  });
  const noteId = created.json().id;

  const refused = await app.server.inject({
    method: 'PATCH', url: `/tasks/tache-a/notes/${noteId}`,
    payload: { title: 'la nuit du grain' },
  });
  expect(refused.statusCode).toBe(422);
  expect(refused.json().error.code).toBe('ATTRIBUTION_PREFIX_REMOVED');

  const accepted = await app.server.inject({
    method: 'PATCH', url: `/tasks/tache-a/notes/${noteId}`,
    payload: { title: 'journal de bord, page 3 du 09/07/1998 — la nuit du grain' },
  });
  expect(accepted.statusCode).toBe(200);
});
```

Dans `server/src/http/tasks_controller.ts`, avant `patchTaskNote` : lire la note courante, et si `titleKeepsPrefix(courant.title, patch.title)` est faux, lever
`new AppError(ErrorCode.ATTRIBUTION_PREFIX_REMOVED, "le préfixe d'attribution d'une note ne peut pas être retiré", 422, { noteId, prefix })`.
Ajouter `ATTRIBUTION_PREFIX_REMOVED` au module d'énumérations partagé.

- [ ] **Step 6: Lancer, vérifier, commiter**

Run: `cd server && npm run test:integration -- tasks_controller && npm run typecheck`

```bash
git add server/src/metier/tasks/note_title.ts server/src/metier/tasks/note_title.test.ts \
        server/src/http/tasks_controller.ts server/src/http/tasks_controller.itest.ts \
        src/shared/enums.ts docs/api-contract.md
git commit -m "feat(server): le préfixe d'attribution d'une note est verrouillé au serveur"
```

### Task 4 : Le répertoire de livraison d'une tâche

**Files:**
- Modify: `server/src/contract/task_interface.ts`
- Modify: `server/src/http/tasks_controller.ts:70-85`
- Modify: `server/src/repository/task_repository.ts`
- Test: `server/src/http/tasks_controller.itest.ts`

**Interfaces:**
- Produces: `TaskPatchInput.exportDirectory?: string | null`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('le répertoire de livraison est réglable, confiné sous TASKS_ROOT', async () => {
  const ok = await app.server.inject({
    method: 'PATCH', url: '/tasks/tache-a',
    payload: { exportDirectory: 'bd/le-grand-depart' },
  });
  expect(ok.statusCode).toBe(200);
  expect(ok.json().exportDirectory).toBe(`${config.tasksRoot}/bd/le-grand-depart`);
});

test('un répertoire qui sort de TASKS_ROOT est refusé, pas assaini en silence', async () => {
  for (const directory of ['../ailleurs', '/etc', 'bd/../../ailleurs']) {
    const refused = await app.server.inject({
      method: 'PATCH', url: '/tasks/tache-a', payload: { exportDirectory: directory },
    });
    expect(refused.statusCode).toBe(422);
    expect(refused.json().error.code).toBe('DIRECTORY_OUTSIDE_ROOT');
  }
});

test('remettre le répertoire à null rend le défaut <TASKS_ROOT>/<slug>', async () => {
  await app.server.inject({ method: 'PATCH', url: '/tasks/tache-a',
                            payload: { exportDirectory: 'bd/ailleurs' } });
  const reset = await app.server.inject({ method: 'PATCH', url: '/tasks/tache-a',
                                          payload: { exportDirectory: null } });
  expect(reset.json().exportDirectory).toBe(`${config.tasksRoot}/tache-a`);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- tasks_controller`
Expected: FAIL — `UNKNOWN_PARAMETER` ou `exportDirectory` ignoré

- [ ] **Step 3: Implémenter**

`TaskPatchInput` reçoit `readonly exportDirectory?: string | null;`. Dans `parsePatchInput`, accepter la clé et la valider par résolution :

```typescript
function resolveExportDirectory(raw: string, tasksRoot: string): string {
  const resolved = path.resolve(tasksRoot, raw);
  const root = path.resolve(tasksRoot);
  // `startsWith` seul accepterait `<root>-autre` ; le séparateur ferme la faille.
  if (resolved !== root && !resolved.startsWith(root + path.sep)) {
    throw new AppError(ErrorCode.DIRECTORY_OUTSIDE_ROOT,
      'le répertoire de livraison doit rester sous TASKS_ROOT', 422,
      { directory: raw, root });
  }
  return resolved;
}
```

La lecture d'une tâche renvoie `export_directory` s'il est posé, sinon `<TASKS_ROOT>/<slug>`.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- tasks_controller`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/contract/task_interface.ts server/src/http/tasks_controller.ts \
        server/src/repository/task_repository.ts server/src/http/tasks_controller.itest.ts \
        docs/api-contract.md
git commit -m "feat(server): le répertoire de livraison d'une tâche, confiné sous TASKS_ROOT"
```

### Task 5 : La période du site devient une borne de début

**Files:**
- Modify: `server/src/http/ref_controller.ts:120-140`
- Modify: `server/src/contract/text_interface.ts`
- Test: `server/src/repository/album_span.itest.ts` (section web)

**Interfaces:**
- Produces: `PUT /ref/web-span` accepte `{ documentId, dateFrom, note? }` — `dateTo` disparaît de l'entrée.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('une seule borne suffit, et la fin vient du document daté suivant', async () => {
  await putWebSpan(client, { documentId: 'web/1999/Transat',      dateFrom: '1999-11-10', note: null });
  await putWebSpan(client, { documentId: 'web/1999/Caraibe',      dateFrom: '2000-01-01', note: null });
  await putWebSpan(client, { documentId: 'web/1999/VersTrinidad', dateFrom: '2000-01-15', note: null });

  const rows = await listWebDocuments(client);
  const transat = rows.find((r) => r.documentId === 'web/1999/Transat');
  // La fin est celle du suivant, exclusive d'un jour : deux périodes ne se chevauchent pas.
  expect(transat?.span).toMatchObject({ start: '1999-11-10', end: '1999-12-31', kind: 'inference' });

  const dernier = rows.find((r) => r.documentId === 'web/1999/VersTrinidad');
  expect(dernier?.span).toMatchObject({ start: '2000-01-15', end: '2000-01-15' });
});

test('un document non daté hérite du dernier daté qui le précède', async () => {
  await putWebSpan(client, { documentId: 'web/1999/Transat', dateFrom: '1999-11-10', note: null });
  const rows = await listWebDocuments(client);
  const suivant = rows.find((r) => r.documentId === 'web/1999/Caraibe');
  expect(suivant?.span).toMatchObject({ start: '1999-11-10', kind: 'inference' });
  expect(suivant?.spanSource).toBe('carried');
});

test('un document antérieur à la première date saisie reste sans date', async () => {
  await putWebSpan(client, { documentId: 'web/1999/Caraibe', dateFrom: '2000-01-01', note: null });
  const rows = await listWebDocuments(client);
  expect(rows.find((r) => r.documentId === 'web/1900-1988')?.span).toBeNull();
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- album_span`
Expected: FAIL — `dateTo` toujours obligatoire

- [ ] **Step 3: Implémenter**

`parseWebSpanInput` n'accepte plus `dateTo`. `listWebDocuments` ordonne les documents datés par `date_from`, puis attribue à chacun une fin = *(début du suivant) − 1 jour*, et au dernier une fin égale à son propre début. Les documents non datés qui suivent un document daté prennent son intervalle avec `spanSource: 'carried'` ; ceux qui précèdent la première date restent `span: null`.

L'ordre des non datés entre deux datés est celui du `document_id`, qui est stable — c'est le seul ordre disponible et il ne sert qu'à répartir dans un intervalle déjà borné.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- album_span && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/http/ref_controller.ts server/src/contract/text_interface.ts \
        server/src/repository/album_span.itest.ts docs/api-contract.md
git commit -m "feat(server): la période d'un document du site est une borne de début"
```

### Task 6 : Annoncer le contrat

- [ ] **Step 1: Vérifier que les quatre amendements sont écrits**

Run: `grep -c "^| \*\*A[0-9]" docs/api-contract.md`
Expected: 7 (les trois d'origine + A4 note dérivée + A5 verrou de titre + A6 répertoire + A7 borne unique)

- [ ] **Step 2: Écrire aux deux agents**

Message court, format `RE:` / `DONE:` / `DETAIL:`, nommant les quatre formes et leurs champs exacts. Le frontend ne code contre elles qu'après ce message.

- [ ] **Step 3: Commit**

```bash
git add docs/api-contract.md ETAT-TRAVAUX.md
git commit -m "docs: les quatre amendements de la 1.5, annoncés"
```

---

## Tranche B — La date d'une page

**Livrable :** chaque page porte une date avec sa nature, selon la cascade registre → notes → héritage.

**Peut démarrer :** en parallèle de la Tranche A, sauf la Task 8 qui a besoin de la migration.

### Task 7 : La cascade, pure

**Files:**
- Create: `server/src/metier/pages/page_date.ts`
- Create: `server/src/metier/pages/page_date.test.ts`

**Interfaces:**
- Produces: `resolvePageDates(pages: readonly PageInput[]): readonly ResolvedPageDate[]`, avec
  `PageInput = { pageId: string; ordinal: number; registerDates: readonly string[]; noteDates: readonly string[] }`
  et `ResolvedPageDate = { pageId: string; start: string; end: string; source: 'register' | 'notes' | 'carried' }`.

- [ ] **Step 1: Écrire le test qui échoue**

`server/src/metier/pages/page_date.test.ts` :

```typescript
import { resolvePageDates } from './page_date.ts';

const page = (ordinal: number, registerDates: string[], noteDates: string[] = []) =>
  ({ pageId: `d/p${String(ordinal).padStart(3, '0')}`, ordinal, registerDates, noteDates });

test('le registre date la page', () => {
  const [r] = resolvePageDates([page(1, ['1998-07-09', '1998-07-19'])]);
  expect(r).toEqual({ pageId: 'd/p001', start: '1998-07-09', end: '1998-07-19', source: 'register' });
});

test('les notes du haut ne déplacent jamais une date que le registre établit', () => {
  // Un billet de musée collé après coup, daté un mois plus tôt.
  const [r] = resolvePageDates([page(1, ['1999-08-04', '1999-08-07'], ['1999-07-01'])]);
  expect(r.start).toBe('1999-08-04');
  expect(r.source).toBe('register');
});

test('une page sans registre prend la date lue dans ses notes', () => {
  const [r] = resolvePageDates([page(1, [], ['1998-07-08'])]);
  expect(r).toEqual({ pageId: 'd/p001', start: '1998-07-08', end: '1998-07-08', source: 'notes' });
});

test('une page sans rien hérite de la précédente', () => {
  const rs = resolvePageDates([page(1, ['1999-08-04']), page(2, [], [])]);
  expect(rs[1]).toEqual({ pageId: 'd/p002', start: '1999-08-04', end: '1999-08-04', source: 'carried' });
});

test('l’héritage ne remonte jamais le temps : les pages avant la première datée restent sans date', () => {
  const rs = resolvePageDates([page(1, [], []), page(2, [], []), page(3, ['1999-08-04'])]);
  expect(rs.map((r) => r.pageId)).toEqual(['d/p003']);
});

test('l’ordre de traitement est celui des ordinaux, pas celui du tableau reçu', () => {
  const rs = resolvePageDates([page(3, [], []), page(1, ['1999-08-04']), page(2, [], [])]);
  expect(rs.map((r) => [r.pageId, r.source])).toEqual([
    ['d/p001', 'register'], ['d/p002', 'carried'], ['d/p003', 'carried'],
  ]);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm test -- page_date`
Expected: FAIL — module introuvable

- [ ] **Step 3: Implémenter**

```typescript
export interface PageInput {
  readonly pageId: string;
  readonly ordinal: number;
  /** Dates lues dans le registre réglé — le document officiel, qui fait autorité. */
  readonly registerDates: readonly string[];
  /** Dates lues dans les notes libres. Ne servent QUE si le registre est muet. */
  readonly noteDates: readonly string[];
}

export interface ResolvedPageDate {
  readonly pageId: string;
  readonly start: string;
  readonly end: string;
  readonly source: 'register' | 'notes' | 'carried';
}

/**
 * Registre d'abord, à défaut les notes, à défaut la page précédente. Une page
 * antérieure à la première page datée n'a pas de date : l'héritage ne remonte
 * jamais le temps, ici comme partout ailleurs dans l'application.
 */
export function resolvePageDates(pages: readonly PageInput[]): readonly ResolvedPageDate[] {
  const ordered = [...pages].sort((a, b) => a.ordinal - b.ordinal);
  const out: ResolvedPageDate[] = [];
  let carried: { start: string; end: string } | null = null;

  for (const page of ordered) {
    const own = page.registerDates.length > 0
      ? { dates: page.registerDates, source: 'register' as const }
      : page.noteDates.length > 0
        ? { dates: page.noteDates, source: 'notes' as const }
        : null;

    if (own !== null) {
      const start = own.dates.reduce((a, b) => (a < b ? a : b));
      const end = own.dates.reduce((a, b) => (a > b ? a : b));
      out.push({ pageId: page.pageId, start, end, source: own.source });
      carried = { start, end };
    } else if (carried !== null) {
      out.push({ pageId: page.pageId, start: carried.start, end: carried.end, source: 'carried' });
    }
  }
  return out;
}
```

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm test -- page_date`
Expected: PASS, 6 tests

- [ ] **Step 5: Commit**

```bash
git add server/src/metier/pages/page_date.ts server/src/metier/pages/page_date.test.ts
git commit -m "feat(server): la cascade registre puis notes puis héritage, pure"
```

### Task 8 : Peupler et servir la date de page

**Files:**
- Create: `server/src/repository/page_date_repository.ts`
- Create: `server/src/repository/page_date_repository.itest.ts`
- Modify: `server/src/import/import_service.ts` (fin d'import)
- Modify: `server/src/contract/text_interface.ts` (`TextPage.date`)
- Modify: `server/src/repository/text_repository.ts` (projection des pages)

**Interfaces:**
- Consumes: `resolvePageDates` (Task 7), la table `app.page_date` (Task 1).
- Produces: `recomputePageDates(client): Promise<number>` ; `TextPage.date: ResolvedDate | null`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('les 52 pages du journal reçoivent une date, aucune n’en manque', async () => {
  await recomputePageDates(client);
  const { rows } = await client.query(`
    SELECT count(*)::int n FROM pipeline.page p
     WHERE p.document_id = 'logbook'
       AND NOT EXISTS (SELECT 1 FROM app.page_date d WHERE d.page_id = p.id)`);
  expect(rows[0].n).toBe(0);
});

test('les pages 1, 2 et 31 du journal sont datées par leurs notes, pas par un registre', async () => {
  await recomputePageDates(client);
  const { rows } = await client.query(`
    SELECT p.ordinal, d.source, d.date_start::text
      FROM app.page_date d JOIN pipeline.page p ON p.id = d.page_id
     WHERE p.document_id = 'logbook' AND p.ordinal IN (1, 2, 31) ORDER BY p.ordinal`);
  expect(rows).toEqual([
    { ordinal: 1,  source: 'notes', date_start: '1998-07-08' },
    { ordinal: 2,  source: 'notes', date_start: '1998-07-08' },
    { ordinal: 31, source: 'notes', date_start: '2000-03-02' },
  ]);
});

test('les 22 pages héritées de « Ma vie » sont marquées carried', async () => {
  await recomputePageDates(client);
  const { rows } = await client.query(`
    SELECT count(*)::int n FROM app.page_date d JOIN pipeline.page p ON p.id = d.page_id
     WHERE p.document_id = 'ma-vie' AND d.source = 'carried'`);
  expect(rows[0].n).toBe(22);
});

test('GET /pages sert la date avec sa nature : lue en register/notes, inférée en carried',
  async () => {
    await recomputePageDates(client);
    const response = await app.server.inject({ method: 'GET', url: '/pages?documentId=ma-vie' });
    const items = response.json().items;
    const propre = items.find((p) => p.ordinal === 1);
    const herite = items.find((p) => p.ordinal === 2);
    expect(propre.date.kind).toBe('reading');
    expect(herite.date.kind).toBe('inference');
  });
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- page_date_repository`
Expected: FAIL — `recomputePageDates` n'existe pas

- [ ] **Step 3: Implémenter**

`recomputePageDates` lit, par document manuscrit, l'ordinal de chaque page, les dates de ses `log_entry` et celles de ses `passage`, appelle `resolvePageDates`, et réécrit `app.page_date` en une transaction (`DELETE` puis insertion en lot). Le seul document qui a un registre est `logbook` ; pour `ma-vie` la liste `registerDates` est vide et les passages remplissent `noteDates`, ce qui redonne bien le comportement attendu sans cas particulier dans le code.

Appelé en fin d'import, après l'insertion de `pipeline.text_unit`.

`TextPage.date` est un `ResolvedDate` de `kind: 'reading'` quand `source` vaut `register` ou `notes`, et `kind: 'inference'` quand il vaut `carried`. `TextPage.window` reste servi tel quel : c'est la fenêtre de la pipeline amont, elle ne ment pas, elle répond juste à une autre question.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run db:import && npm run test:integration -- page_date && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/repository/page_date_repository.ts server/src/repository/page_date_repository.itest.ts \
        server/src/import/import_service.ts server/src/contract/text_interface.ts \
        server/src/repository/text_repository.ts docs/api-contract.md
git commit -m "feat(server): chaque page porte sa date résolue et sa nature"
```

---

## Tranche C — Les vignettes de page

**Livrable :** une liste de 103 pages coûte quelques centaines de kilo-octets au lieu de 31 Mo.

**Peut démarrer :** en parallèle des tranches A et B. Ne dépend d'aucune des deux.

### Task 9 : Réduire un scan, avec cache

**Files:**
- Create: `server/src/metier/pages/thumb_service.ts`
- Create: `server/src/metier/pages/thumb_service.test.ts`
- Modify: `server/src/http/texts_controller.ts`
- Test: `server/src/http/texts_controller.itest.ts`

**Interfaces:**
- Consumes: `config.pagesRoot`, `config.renderCacheRoot`, le sémaphore `InFlightRenders` déjà construit dans `bootstrap.ts`.
- Produces: `GET /pages/thumb?pageId=…&edge=…` → `image/jpeg`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('la vignette est le scan ENTIER réduit — même rapport d’aspect, aucun rognage', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/pages/thumb?pageId=logbook%2Fp010&edge=320',
  });
  expect(response.statusCode).toBe(200);
  expect(response.headers['content-type']).toBe('image/jpeg');
  const { width, height } = await readJpegSize(response.rawPayload);
  expect(Math.max(width, height)).toBe(320);
  // Le scan mesure 830×1282 ; le rapport doit être conservé à un pixel près.
  expect(Math.abs(width / height - 830 / 1282)).toBeLessThan(0.01);
});

test('une seconde demande sert le cache, sans relancer de rendu', async () => {
  await app.server.inject({ method: 'GET', url: '/pages/thumb?pageId=logbook%2Fp010&edge=320' });
  const renders = countRenders();
  await app.server.inject({ method: 'GET', url: '/pages/thumb?pageId=logbook%2Fp010&edge=320' });
  expect(countRenders()).toBe(renders);
});

test('une taille non prévue est refusée, jamais rendue', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/pages/thumb?pageId=logbook%2Fp010&edge=4000',
  });
  expect(response.statusCode).toBe(400);
  expect(response.json().error.code).toBe('INVALID_PARAMETER');
});

test('une page inconnue est un 404 nommé, pas une image vide', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/pages/thumb?pageId=nowhere%2Fp001&edge=320',
  });
  expect(response.statusCode).toBe(404);
  expect(response.json().error.code).toBe('NOT_FOUND');
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- texts_controller`
Expected: FAIL — route inconnue, 404 `NOT_FOUND` sur `/pages/thumb`

- [ ] **Step 3: Implémenter**

`thumb_service.ts` réutilise exactement le mécanisme des rendus de photos : `sips` derrière le même `InFlightRenders`, cache sous `RENDER_CACHE_ROOT/pages/<pageId sanitisé>-<edge>.jpg`, écriture via `safeFs`. `edge` est une valeur **fermée** — `{ 160, 320, 640 }` — parce qu'une valeur libre laisserait un visiteur remplir le disque de variantes.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- texts_controller && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/metier/pages/thumb_service.ts server/src/metier/pages/thumb_service.test.ts \
        server/src/http/texts_controller.ts server/src/http/texts_controller.itest.ts \
        server/src/runtime/bootstrap.ts docs/api-contract.md
git commit -m "feat(server): les vignettes de page, réduites et mises en cache"
```

---

## Tranche D — L'écran de datation du site

**Livrable :** l'écran s'ouvre sur des dates proposées qui disent ce qui les soutient.

**Doit attendre :** la Task 5 (borne unique).

### Task 10 : La date proposée et son support

**Files:**
- Create: `server/src/repository/web_proposal_repository.ts`
- Create: `server/src/repository/web_proposal_repository.itest.ts`
- Modify: `server/src/contract/text_interface.ts`
- Modify: `server/src/http/ref_controller.ts`

**Interfaces:**
- Produces: `WebDocumentRow.proposal: WebDateProposal | null` avec
  `{ date: string; photoCount: number; datedToDayCount: number; spanDays: number }`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('une page propose la plus petite date de ses photos liées, et dit ce qui la soutient',
  async () => {
    const rows = await listWebDocuments(client);
    const gal15 = rows.find((r) => r.documentId === 'web/2003/2003_gal_15');
    expect(gal15?.proposal).toEqual({
      date: '2004-10-05', photoCount: 20, datedToDayCount: 20, spanDays: 9,
    });
  });

test('une proposition fragile le dit : une seule photo, datée au mois', async () => {
  const rows = await listWebDocuments(client);
  const photo = rows.find((r) => r.documentId === 'web/photo');
  expect(photo?.proposal).toMatchObject({ photoCount: 1, datedToDayCount: 0 });
});

test('un document sans photo liée n’a aucune proposition — jamais une date inventée', async () => {
  const rows = await listWebDocuments(client);
  expect(rows.find((r) => r.documentId === 'web/1999-2002')?.proposal).toBeNull();
});

test('la proposition ne remplit jamais la période : les deux champs sont indépendants', async () => {
  const rows = await listWebDocuments(client);
  const gal15 = rows.find((r) => r.documentId === 'web/2003/2003_gal_15');
  expect(gal15?.proposal).not.toBeNull();
  expect(gal15?.span).toBeNull();
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- web_proposal`
Expected: FAIL — `proposal` n'existe pas

- [ ] **Step 3: Implémenter**

```sql
SELECT 'web/' || regexp_replace(l.page, '\.html?$', '') AS document_id,
       min(p.resolved_start)::text                      AS date,
       count(*)::int                                    AS photo_count,
       count(*) FILTER (WHERE p.resolved_precision = 'day')::int AS dated_to_day_count,
       (max(p.resolved_end) - min(p.resolved_start))::int        AS span_days
  FROM app.web_gallery_link l
  JOIN pipeline.photo p ON p.sha256 = l.sha256
 WHERE p.resolved_start IS NOT NULL
 GROUP BY 1
```

Joint en `LEFT JOIN` sur la liste des documents : un document sans lien garde `proposal: null`. Le résultat ne touche jamais `ref.web_span` — la proposition s'affiche, elle ne se saisit pas.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- web_proposal && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/repository/web_proposal_repository.ts \
        server/src/repository/web_proposal_repository.itest.ts \
        server/src/contract/text_interface.ts server/src/http/ref_controller.ts docs/api-contract.md
git commit -m "feat(server): la date proposée d'une page du site, avec ce qui la soutient"
```

### Task 11 : Le périmètre de la liste

**Files:**
- Modify: `server/src/http/ref_controller.ts:111-119`
- Test: `server/src/repository/web_proposal_repository.itest.ts`

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('la liste se limite au périmètre, les rebuts derrière un drapeau explicite', async () => {
  const dans = await listWebDocuments(client, { scope: 'perimeter' });
  expect(dans.map((r) => r.documentId)).not.toContain('web/googlea0ccc7e24963cc5e');
  expect(dans.map((r) => r.documentId)).not.toContain('web/2005/3/raiders/nico');
  expect(dans.map((r) => r.documentId)).toContain('web/1999/Transat');
  expect(dans.length).toBeLessThan(30);

  const tout = await listWebDocuments(client, { scope: 'all' });
  expect(tout.length).toBe(60);
});
```

- [ ] **Step 2: Lancer, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- web_proposal`
Expected: FAIL — `scope` inconnu

- [ ] **Step 3: Implémenter**

`GET /ref/web-documents?scope=perimeter|all`, défaut `perimeter`. Le périmètre est un document dont **le chemin ou la proposition** tombe dans `config.periodFrom`…`periodTo`, et qui porte au moins deux passages. Le seuil de deux écarte `bidon`, `test/map` et la vérification Google sans nommer aucun fichier en dur — un filtre par liste de noms se périmerait au premier réimport.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- web_proposal`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/http/ref_controller.ts server/src/repository/web_proposal_repository.itest.ts \
        docs/api-contract.md
git commit -m "feat(server): la liste des documents du site se limite au périmètre"
```

---

## Tranche E — Les filtres de l'écran Textes

**Livrable :** le frontend peut construire ses filtres sans deviner ce que la donnée contient.

**Peut démarrer :** en parallèle des tranches C et D.

### Task 12 : Le compte réel des textes écartés

**Files:**
- Modify: `server/src/http/texts_controller.ts:142-158`
- Modify: `server/src/repository/text_repository.ts`
- Test: `server/src/repository/text_repository.itest.ts`

**Interfaces:**
- Produces: `ListEnvelope.populationTotal` et `excludedCount` renseignés pour `/texts`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('un filtre de date dit combien de textes sans date il a écartés', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/texts?documentId=logbook&dateFrom=1999-01-01&dateTo=1999-12-31',
  });
  const body = response.json();
  // 341 unités du journal ne portent aucune date : elles sont écartées, et comptées.
  expect(body.excludedCount).toBe(341);
  expect(body.populationTotal).toBe(body.total + body.excludedCount);
});

test('sans filtre de date, rien n’est écarté', async () => {
  const response = await app.server.inject({ method: 'GET', url: '/texts?documentId=logbook' });
  expect(response.json().excludedCount).toBe(0);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- text_repository`
Expected: FAIL — `excludedCount` vaut 0, il est codé en dur dans le contrôleur

- [ ] **Step 3: Implémenter**

`listTexts` renvoie en plus `undatedExcluded` : le compte des unités qui satisfont **tous les autres filtres** mais que la clause de date écarte pour absence de date. Le contrôleur cesse de coder `excludedCount: 0`.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- text_repository`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/http/texts_controller.ts server/src/repository/text_repository.ts \
        server/src/repository/text_repository.itest.ts
git commit -m "fix(server): GET /texts compte enfin les textes sans date qu'il écarte"
```

### Task 13 : Les facettes de dates

**Files:**
- Create: `server/src/repository/text_facets.ts`
- Create: `server/src/repository/text_facets.itest.ts`
- Modify: `server/src/http/texts_controller.ts`

**Interfaces:**
- Produces: `GET /texts/facets?documentId=…` → `{ years: Bucket[]; months: Bucket[]; days: Bucket[] }`, chaque `Bucket = { value: string; count: number }`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('« Ma vie » ne propose qu’une année et quatre mois — ce que la donnée contient', async () => {
  const response = await app.server.inject({ method: 'GET', url: '/texts/facets?documentId=ma-vie' });
  const facets = response.json();
  expect(facets.years.map((b) => b.value)).toEqual(['1999']);
  expect(facets.months.map((b) => b.value)).toEqual(['1999-08', '1999-09', '1999-10', '1999-11']);
  expect(facets.days.length).toBe(81);
});

test('le journal propose cinq années', async () => {
  const response = await app.server.inject({ method: 'GET', url: '/texts/facets?documentId=logbook' });
  expect(response.json().years.map((b) => b.value))
    .toEqual(['1998', '1999', '2000', '2001', '2002']);
});

test('une facette porte son compte de textes, jamais un compte de jours', async () => {
  const response = await app.server.inject({ method: 'GET', url: '/texts/facets?documentId=ma-vie' });
  const annee = response.json().years[0];
  expect(annee.count).toBe(677);
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- text_facets`
Expected: FAIL — route inconnue

- [ ] **Step 3: Implémenter**

Trois agrégats sur `pipeline.text_unit` filtré par `document_id`, groupés par `to_char(date_start, 'YYYY')`, `'YYYY-MM'` et `'YYYY-MM-DD'`, `date_start IS NOT NULL`. Même `ParamSpec` que `/texts` pour `documentId`.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- text_facets && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/repository/text_facets.ts server/src/repository/text_facets.itest.ts \
        server/src/http/texts_controller.ts docs/api-contract.md
git commit -m "feat(server): les années, mois et jours que chaque source contient réellement"
```

### Task 14 : Les pages qui correspondent à un filtre

**Files:**
- Modify: `server/src/http/texts_controller.ts:99-111`
- Test: `server/src/repository/text_repository.itest.ts`

**Interfaces:**
- Produces: `GET /pages` accepte `dateFrom`, `dateTo`, `q` — mêmes noms et même sémantique que `/texts`.

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
test('une page sort dès qu’un de ses textes tombe dans la plage', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/pages?documentId=ma-vie&dateFrom=1999-08-04&dateTo=1999-08-06',
  });
  const ordinals = response.json().items.map((p) => p.ordinal);
  expect(ordinals).toContain(1);
  expect(ordinals).not.toContain(103);
});

test('la recherche par mot renvoie les pages qui le contiennent, avec leur compte', async () => {
  const response = await app.server.inject({
    method: 'GET', url: '/pages?documentId=ma-vie&q=mouillage',
  });
  const items = response.json().items;
  expect(items.length).toBeGreaterThan(0);
  expect(items.every((p) => p.matchCount > 0)).toBe(true);
});

test('un paramètre inconnu reste un 400 nommé', async () => {
  const response = await app.server.inject({ method: 'GET', url: '/pages?dateFron=1999-01-01' });
  expect(response.statusCode).toBe(400);
  expect(response.json().error.code).toBe('UNKNOWN_PARAMETER');
});
```

- [ ] **Step 2: Lancer le test, vérifier qu'il échoue**

Run: `cd server && npm run test:integration -- text_repository`
Expected: FAIL — `UNKNOWN_PARAMETER: dateFrom` sur `/pages`

- [ ] **Step 3: Implémenter**

Le `ParamSpec` de `/pages` gagne `dateFrom`, `dateTo`, `q`. Une page est retenue si **un de ses textes** satisfait le filtre — un `EXISTS`, jamais un `IN` sur une liste construite en TypeScript. `matchCount` n'est renseigné que quand `q` est présent, comme `highlights` sur `/texts`.

- [ ] **Step 4: Lancer, vérifier**

Run: `cd server && npm run test:integration -- text_repository`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/http/texts_controller.ts server/src/repository/text_repository.ts \
        server/src/repository/text_repository.itest.ts docs/api-contract.md
git commit -m "feat(server): GET /pages filtre par période et par mot"
```

---

## Tranche F — Vérification contre le vrai serveur

**Livrable :** la preuve que le client et le serveur sont d'accord sur les formes, pas seulement que chaque moitié passe ses propres tests.

**Doit attendre :** toutes les tranches précédentes, et la tranche 1 du plan frontend.

### Task 15 : L'invariant de forme

**Files:**
- Create: `server/src/invariants/contract_shapes.itest.ts`

**Pourquoi cette tâche existe :** les défauts les plus coûteux de la V1 n'étaient pas des bugs de logique, c'étaient des désaccords de forme entre client et serveur — un tableau d'identifiants là où le serveur renvoyait des comptes, un `202` asynchrone là où le client attendait un `409`. Les deux suites étaient vertes. Ce test répond à la seule question qu'aucune des deux ne posait : *ce que le serveur renvoie vraiment satisfait-il le schéma que le client applique vraiment ?*

- [ ] **Step 1: Écrire le test qui échoue**

```typescript
import { TaskDetailSchema } from '../../../src/api/contract/task.ts';
import { TextPageListSchema } from '../../../src/api/contract/text.ts';
import { WebDocumentListSchema } from '../../../src/api/contract/ref.ts';

// Les schémas Zod du CLIENT, appliqués aux réponses du VRAI serveur.
test.each([
  ['/tasks/tache-a',                      TaskDetailSchema],
  ['/pages?documentId=ma-vie',            TextPageListSchema],
  ['/ref/web-documents',                  WebDocumentListSchema],
])('%s satisfait le schéma que le client applique', async (url, schema) => {
  const response = await app.server.inject({ method: 'GET', url });
  expect(response.statusCode).toBe(200);
  const parsed = schema.safeParse(response.json());
  expect(parsed.error?.issues ?? []).toEqual([]);
});
```

- [ ] **Step 2: Lancer, vérifier qu'il échoue si une forme diverge**

Run: `cd server && npm run test:integration -- contract_shapes`
Expected: PASS si les deux plans sont d'accord ; sinon la liste exacte des champs qui divergent, ce qui est le résultat utile

- [ ] **Step 3: Corriger le côté fautif**

Le schéma client et le contrat serveur sont la même vérité écrite deux fois. Quand ils divergent, `server/src/contract/*.ts` fait foi, sauf si c'est lui qui s'est trompé.

- [ ] **Step 4: Commit**

```bash
git add server/src/invariants/contract_shapes.itest.ts
git commit -m "test(server): les réponses réelles satisfont les schémas réels du client"
```

---

## Ce qui peut se faire en parallèle

Les deux agents travaillent sur `test_dev`, le frontend dans `src/`, le backend dans `server/`. Les commits se font par chemins explicites.

```
Tranche A (contrat)  ─── bloque le frontend jusqu'à la Task 6
Tranche B (dates)    ─┐
Tranche C (vignettes) ├── en parallèle entre elles, dès le départ
Tranche E (filtres)  ─┘
Tranche D (datation)  ─── après la Task 5
Tranche F (formes)    ─── en dernier, avec le frontend
```

Le seul fichier partagé entre les deux plans est `src/shared/enums.ts` : le backend y ajoute `ATTRIBUTION_PREFIX_REMOVED` et `DIRECTORY_OUTSIDE_ROOT` en Task 3 et Task 4. Le frontend ne le touche pas avant la Task 6.

## Plan de tests

| Niveau | Ce qu'il prouve | Où |
|:--|:--|:--|
| **Unitaire** (`*.test.ts`) | La cascade de dates, le verrou de préfixe, le calcul de vignette. Aucune base, aucun réseau. | `metier/` |
| **Intégration** (`*.itest.ts`) | Les requêtes SQL contre la vraie base, les routes contre le vrai Fastify. | `repository/`, `http/` |
| **Invariants** (`invariants/*.itest.ts`) | Ce qui ne doit jamais devenir faux : le schéma, l'absence d'écriture hors racines, et désormais l'accord de forme client/serveur. | `invariants/` |

Chaque tâche ci-dessus porte son test avant son implémentation, et le fait échouer avant de le faire passer. Les chiffres attendus dans les tests d'intégration — 341 unités sans date, 22 pages héritées, 20 photos pour `2003_gal_15` — viennent de la base réelle et ont été mesurés ; s'ils bougent, c'est que la donnée a bougé, et c'est une information, pas un test à ajuster.

## Self-review

**Couverture de la spec.** Les quatre amendements → tranche A. La date d'une page → tranche B. Les vignettes → tranche C. L'écran de datation → tranche D. Les filtres et facettes → tranche E. Les textes de la Revue sont déjà servis par `GET /tasks/:slug/review`, aucune tâche n'est nécessaire. Le seuil de 40 caractères sur les fragments du site et le signe sur les pages de registre suspectes sont du rendu : ils sont dans le plan frontend.

**Cohérence des types.** `ResolvedPageDate.source` (`register|notes|carried`) est la valeur écrite dans `app.page_date.source` et lue par `TextPage.date.kind`. `TaskTextRef` est la forme déjà au contrat, réutilisée par `TaskNote.derivedFrom`. `WebDateProposal` n'est produit qu'en Task 10 et consommé que par le frontend.
